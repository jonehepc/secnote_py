---
phase: 04-rich-text-editor
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .venv
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10, EDIT-11]
must_haves:
  truths:
    - "执行 Phase 04 任一测试前，项目虚拟环境 .venv 存在且可运行 pytest"
    - "所有后续 verify 命令使用 .venv/bin/python，不使用系统 Python"
    - ".venv 是本地执行环境，不进入 git 提交"
  artifacts:
    - path: ".venv/bin/python"
      provides: "Phase 04 测试运行 Python 解释器"
      contains: "venv interpreter"
    - path: ".venv/bin/pytest"
      provides: "pytest 控制台入口，由 pip 安装 pytest 提供"
      contains: "pytest"
  key_links:
    - from: "04-00-PLAN.md"
      to: "后续 04-01~04-06 PLAN verify 命令"
      via: "depends_on 链路和 .venv/bin/python"
      pattern: "\.venv/bin/python -m pytest"
---

<objective>
为 Phase 04 建立测试执行前置环境：创建项目本地 `.venv`，安装项目依赖和 pytest，并验证后续计划可使用 `.venv/bin/python` 运行测试。

Purpose: 满足 04-VALIDATION 的 Wave 0 环境要求和用户记忆“在 SecNotepad 中运行 Python 测试/脚本时使用 `.venv`，不要用系统 Python”。
Output: 本地 `.venv` 可用，pytest 可通过 `.venv/bin/python -m pytest --version` 验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-rich-text-editor/04-VALIDATION.md
@.planning/phases/04-rich-text-editor/04-RESEARCH.md
@pyproject.toml
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建并验证 Phase 04 本地虚拟环境</name>
  <files>.venv</files>
  <read_first>
    - .planning/phases/04-rich-text-editor/04-VALIDATION.md
    - .planning/phases/04-rich-text-editor/04-RESEARCH.md
    - pyproject.toml
    - requirements.txt
  </read_first>
  <action>如果仓库根目录不存在 `.venv`，执行 `python -m venv .venv` 创建虚拟环境；如果已存在，则复用现有 `.venv`，不要删除或重建。随后执行 `.venv/bin/python -m pip install -e . pytest` 安装项目依赖和 pytest。不得把 `.venv` 加入 git 暂存或提交；执行后用 `git status --short -- .venv` 确认 `.venv` 不作为计划产物提交。后续 Phase 04 所有 Python/pytest 命令必须使用 `.venv/bin/python`。</action>
  <verify>
    <automated>bash -lc 'if [ ! -x .venv/bin/python ]; then python -m venv .venv; fi && .venv/bin/python -m pip install -e . pytest && .venv/bin/python -m pytest --version && test -x .venv/bin/python'</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python` 存在且可执行。
    - `.venv/bin/python -m pip install -e . pytest` 成功完成。
    - `.venv/bin/python -m pytest --version` 输出 pytest 版本并返回 0。
    - `.venv` 不进入 git 提交；本计划的 SUMMARY 可以提交，`.venv` 本身不得提交。
  </acceptance_criteria>
  <done>Phase 04 的本地 Python 测试环境已准备好，后续 04-01~04-06 可安全使用 `.venv/bin/python -m pytest ...`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI/本地包安装 → `.venv` | 安装依赖会把第三方包写入本地虚拟环境，但不应写入仓库源代码或提交产物。 |
| 系统 Python → 项目 `.venv` | 只允许系统 Python 用于创建虚拟环境；测试和项目脚本运行必须切换到 `.venv/bin/python`。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | `.venv` dependency install | mitigate | 使用项目根目录固定命令 `.venv/bin/python -m pip install -e . pytest`，不修改生产代码或依赖声明。 |
| T-04-00-02 | Information Disclosure | `.venv` local files | mitigate | 明确 `.venv` 只作本地环境，不暂存、不提交，避免把环境缓存或本地路径写入版本库。 |
| T-04-00-03 | Repudiation | test runner selection | mitigate | 所有后续 verify 命令显式使用 `.venv/bin/python`，避免系统 Python 与虚拟环境结果混淆。 |
</threat_model>

<verification>
- `.venv/bin/python -m pytest --version`
- 后续计划必须通过 `depends_on: [04-00]` 或经由 `04-01` 的依赖链获得本环境前置条件。
</verification>

<success_criteria>
- `.venv/bin/python` 可执行。
- `.venv/bin/python -m pytest --version` 返回 0。
- `git status --short -- .venv` 不显示计划要求提交的 tracked 文件；`.venv` 不被纳入提交。
</success_criteria>

<source_audit>
GOAL Phase 4 富文本编辑器：COVERED by preparing the required automated test environment before implementation.
REQ EDIT-01~EDIT-11：COVERED as shared validation prerequisite for all requirement tests.
RESEARCH Environment Availability `.venv` missing：COVERED.
CONTEXT D-65~D-80：INDIRECTLY COVERED as execution prerequisite; no user decision changed.
VALIDATION Wave 0 `.venv` setup：COVERED.
Deferred Ideas：None.
</source_audit>

<output>
After completion, create `.planning/phases/04-rich-text-editor/04-00-SUMMARY.md`
</output>
