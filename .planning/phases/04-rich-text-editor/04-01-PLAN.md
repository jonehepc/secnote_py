---
phase: 04-rich-text-editor
plan: 01
type: tdd
wave: 1
depends_on: [04-00]
files_modified:
  - tests/ui/test_rich_text_editor.py
  - tests/ui/test_main_window.py
  - tests/ui/test_navigation.py
  - tests/model/test_serializer.py
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10, EDIT-11]
must_haves:
  truths:
    - "自动测试先锁定富文本编辑器 EDIT-01~EDIT-11 的可观察行为"
    - "后续实现必须通过安全粘贴、HTML 持久化、工具栏状态同步和缩放不置脏测试"
  artifacts:
    - path: "tests/ui/test_rich_text_editor.py"
      provides: "RichTextEditorWidget 行为测试：字符格式、颜色、段落、列表、待办、缩进、撤销重做、粘贴净化、缩放"
      contains: "test_character_format_actions"
    - path: "tests/ui/test_main_window.py"
      provides: "MainWindow 集成测试：右侧格式工具栏、编辑菜单、视图缩放菜单、页面切换不置脏"
      contains: "test_format_toolbar_is_inside_editor_area"
    - path: "tests/ui/test_navigation.py"
      provides: "跨页面 undo 栈清理和页面切换不误置脏回归测试"
      contains: "test_switching_pages_clears_undo_stack_and_does_not_mark_dirty"
    - path: "tests/model/test_serializer.py"
      provides: "富文本 HTML round-trip 持久化测试"
      contains: "test_rich_text_html_content_roundtrip"
  key_links:
    - from: "tests/ui/test_rich_text_editor.py"
      to: "src/secnotepad/ui/rich_text_editor.py"
      via: "from src.secnotepad.ui.rich_text_editor import RichTextEditorWidget"
      pattern: "RichTextEditorWidget"
    - from: "tests/ui/test_main_window.py"
      to: "src/secnotepad/ui/main_window.py"
      via: "MainWindow fixture"
      pattern: "_rich_text_editor"
---

<objective>
为 Phase 04 建立 RED 测试契约，覆盖用户锁定决策 D-65~D-80、EDIT-01~EDIT-11、04-UI-SPEC 和 04-VALIDATION 中的安全与交互要求。

Purpose: TDD 模式下先写失败测试，防止后续实现把富文本需求简化为静态工具栏或只做部分格式按钮。
Output: 三个测试文件中的失败测试，作为后续计划的实现门。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rich-text-editor/04-CONTEXT.md
@.planning/phases/04-rich-text-editor/04-RESEARCH.md
@.planning/phases/04-rich-text-editor/04-PATTERNS.md
@.planning/phases/04-rich-text-editor/04-VALIDATION.md
@.planning/phases/04-rich-text-editor/04-UI-SPEC.md
@tests/conftest.py
@tests/ui/test_main_window.py
@tests/ui/test_navigation.py
@tests/model/test_serializer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 RichTextEditorWidget RED 行为测试</name>
  <files>tests/ui/test_rich_text_editor.py</files>
  <read_first>
    - tests/conftest.py
    - tests/ui/test_navigation.py
    - tests/ui/test_password_dialog.py
    - .planning/phases/04-rich-text-editor/04-CONTEXT.md
    - .planning/phases/04-rich-text-editor/04-VALIDATION.md
    - .planning/phases/04-rich-text-editor/04-UI-SPEC.md
  </read_first>
  <behavior>
    - `test_editing_page_updates_note_html_and_dirty`: 调用 `RichTextEditorWidget.load_html("<p>旧内容</p>")` 不触发 `content_changed`；随后用 `setPlainText("新内容")` 或 `QTest.keyClicks` 模拟真实编辑后触发 `content_changed`，`to_html()` 包含 `新内容`，并通过 spy/callback 证明 dirty 路径被调用（EDIT-01，D-65）。
    - `test_character_format_actions`: 选中文本后触发 `action_bold`、`action_italic`、`action_underline`、`action_strike`，当前 QTextCursor charFormat 分别反映粗体、斜体、下划线、删除线（EDIT-02，D-66）。
    - `test_font_family_and_size_controls`: 字体控件为 `QFontComboBox`，字号选项精确包含 `8, 9, 10, 11, 12, 14, 16, 18, 20, 24, 28, 32, 36, 48`，选中字号 `18` 后 charFormat 字号为 18（EDIT-03，D-68）。
    - `test_text_and_background_color_actions`: monkeypatch 颜色选择方法返回 `QColor("#ff0000")` 和 `QColor("#ffff00")` 后，选区 charFormat foreground/background 生效（EDIT-04，D-67）。
    - `test_alignment_actions_are_exclusive`: `action_align_left/center/right/justify` 均 checkable 且同属互斥 QActionGroup，触发居中后 `editor.alignment()` 包含 `Qt.AlignCenter`（EDIT-05，D-71/D-72）。
    - `test_list_and_checklist_actions`: 无序/有序列表使用 QTextList，待办按钮插入普通文本 `☐ `，HTML/plain text 不包含 `<input` 或 `checkbox`（EDIT-06，D-73/D-74/D-75）。
    - `test_heading_levels`: 段落样式下拉文本精确为 `正文,H1,H2,H3,H4,H5,H6`，选择 H2 后当前 block `headingLevel()` 为 2（EDIT-07，D-69/D-70/D-72）。
    - `test_indent_outdent_actions`: 增加缩进使当前 block/list indent 增大，减少缩进后不低于 0（EDIT-08，D-76）。
    - `test_undo_redo_actions`: 文本输入后 undo/redo 可用状态随 QTextDocument 信号更新，触发 action 后 plain text 回退/恢复（EDIT-09）。
    - `test_clipboard_actions_and_safe_paste`: 粘贴含 `<img src="file:/tmp/a.png">`、`http://example.com/a.png`、`<script>alert(1)</script>`、`<span onclick="steal()">x</span>`、`<a href="javascript:alert(1)">x</a>` 的 HTML 后，保存 HTML 不含 `<img`、`file:`、`http://`、`https://`、`<script`、`onclick=`、`onerror=`、`javascript:`，状态栏回调收到 `已粘贴文本内容；图片和外部资源未导入`（EDIT-10，安全要求）。
    - `test_zoom_does_not_mark_content_dirty`: `zoom_in/zoom_out/reset_zoom` 改变 `zoom_percent` 为 110/100，不改变 `toHtml()` 内容，不发出内容变化信号（EDIT-11，D-77~D-80）。
  </behavior>
  <action>创建 `tests/ui/test_rich_text_editor.py`。沿用现有 `qapp` fixture，不引入 `pytest-qt`。测试导入 `RichTextEditorWidget` 和 `SafeRichTextEdit`：`from src.secnotepad.ui.rich_text_editor import RichTextEditorWidget, SafeRichTextEdit`。组件 fixture 命名为 `editor_widget`，创建后 `show()`，结束时 `close()` + `deleteLater()`。必须创建 `test_editing_page_updates_note_html_and_dirty`，测试 `load_html("<p>旧内容</p>")` 不触发内容变化、真实编辑 `新内容` 后触发 `content_changed` 且 `to_html()` 包含 `新内容`。颜色测试不得打开真实 `QColorDialog`，通过 monkeypatch `editor_widget._choose_color` 返回固定 `QColor`。`test_clipboard_actions_and_safe_paste` 必须构造包含 `<script>alert(1)</script>`、`onclick="steal()"`、`onerror="steal()"`、`href="javascript:alert(1)"` 的 HTML 输入，并断言输出 HTML 不含 `<script`、`onclick=`、`onerror=`、`javascript:`。RED 阶段运行测试必须失败，失败原因应为模块/类/属性尚不存在或行为尚未实现；不要在本计划创建生产代码。</action>
  <verify>
    <automated>bash -lc 'set +e; QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_rich_text_editor.py -q > /tmp/04-01-task1-red.log 2>&amp;1; code=$?; set -e; if [ $code -eq 0 ]; then echo "Expected RED failure but tests passed"; exit 1; fi; if grep -E "SyntaxError|IndentationError" /tmp/04-01-task1-red.log; then echo "RED failure is syntax-related, not acceptable"; exit 1; fi; grep -E "ModuleNotFoundError|ImportError|AttributeError|AssertionError|FAILED|ERROR" /tmp/04-01-task1-red.log >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'def test_editing_page_updates_note_html_and_dirty'` 输出 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'def test_character_format_actions'` 输出 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'def test_clipboard_actions_and_safe_paste'` 输出 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c '☐ '` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'src="file:'` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c '<script'` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'javascript:'` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'onerror'` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'zoom_percent'` 输出至少 `1`
  </acceptance_criteria>
  <done>RichTextEditorWidget 的 EDIT-02~EDIT-11 行为均有 RED 测试，且没有生产代码实现混入本任务。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 MainWindow 集成 RED 测试</name>
  <files>tests/ui/test_main_window.py, tests/ui/test_navigation.py</files>
  <read_first>
    - tests/ui/test_main_window.py
    - tests/ui/test_navigation.py
    - src/secnotepad/ui/main_window.py
    - .planning/phases/04-rich-text-editor/04-CONTEXT.md
    - .planning/phases/04-rich-text-editor/04-UI-SPEC.md
  </read_first>
  <behavior>
    - `test_format_toolbar_is_inside_editor_area`: 右侧 splitter 第 3 栏内部存在 `_format_toolbar`，主窗口文件工具栏 action 文本不包含 `加粗/斜体/下划线/删除线/文字颜色/背景色`（D-65）。
    - `test_format_toolbar_disabled_without_page_enabled_with_page`: 未选中页面时 `_rich_text_editor.format_toolbar().isEnabled()` 为 False；新建分区和页面并选中后为 True（D-65）。
    - `test_editor_menu_actions_route_to_rich_text_editor`: 编辑菜单 action 文本为 `撤销(&U),重做(&R),剪切(&T),复制(&C),粘贴(&P)`，选中页面后至少粘贴 action 可用，撤销/重做随 document 状态变化（EDIT-09/10）。
    - `test_view_menu_has_zoom_actions`: 视图菜单包含 `放大`、`缩小`、`重置缩放`，快捷键分别为 `Ctrl++` 或 `Ctrl+Plus`、`Ctrl+-`、`Ctrl+0`（D-78）。
    - `test_zoom_actions_keep_note_html_and_dirty_state`: 对当前页面执行放大、缩小、重置后 `note.content` 不变，`window._is_dirty` 仍为 False，状态栏为 `缩放：100%`（D-77/D-79/D-80）。
    - 在 `tests/ui/test_navigation.py` 增加 `test_switching_pages_clears_undo_stack_and_does_not_mark_dirty`，切换页面后 undo 不恢复上一页明文，且 `_is_dirty` 为 False（安全要求）。
  </behavior>
  <action>在 `tests/ui/test_main_window.py` 末尾新增 `class TestRichTextIntegration`，使用现有 `window` fixture。保留现有测试，不删除 `test_edit_actions_disabled`，但若 RED 阶段因新需求冲突可新增新测试表达 Phase 04 期望，后续实现计划再更新旧断言。新增测试必须使用具体属性名 `_rich_text_editor`、`_format_toolbar`、`_act_zoom_in`、`_act_zoom_out`、`_act_zoom_reset`。在 `tests/ui/test_navigation.py` 新增跨页面 undo 栈回归，使用现有 `window_with_notebook` fixture。</action>
  <verify>
    <automated>bash -lc 'set +e; QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_main_window.py tests/ui/test_navigation.py -q > /tmp/04-01-task2-red.log 2>&amp;1; code=$?; set -e; if [ $code -eq 0 ]; then echo "Expected RED failure but tests passed"; exit 1; fi; if grep -E "SyntaxError|IndentationError" /tmp/04-01-task2-red.log; then echo "RED failure is syntax-related, not acceptable"; exit 1; fi; grep -E "_rich_text_editor|_format_toolbar|_act_zoom|AssertionError|FAILED|ERROR" /tmp/04-01-task2-red.log >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/ui/test_main_window.py | grep -c 'class TestRichTextIntegration'` 输出 `1`
    - `grep -v '^#' tests/ui/test_main_window.py | grep -c 'test_format_toolbar_is_inside_editor_area'` 输出 `1`
    - `grep -v '^#' tests/ui/test_main_window.py | grep -c '_act_zoom_reset'` 输出至少 `1`
    - `grep -v '^#' tests/ui/test_navigation.py | grep -c 'test_switching_pages_clears_undo_stack_and_does_not_mark_dirty'` 输出 `1`
  </acceptance_criteria>
  <done>MainWindow 与导航回归的 RED 集成测试覆盖工具栏位置、启用状态、菜单路由、缩放和跨页面 undo 清理。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 添加富文本 HTML 持久化 characterization 测试</name>
  <files>tests/model/test_serializer.py</files>
  <read_first>
    - tests/model/test_serializer.py
    - src/secnotepad/model/snote_item.py
    - src/secnotepad/model/serializer.py
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-rich-text-editor/04-CONTEXT.md
  </read_first>
  <behavior>
    - `test_rich_text_html_content_roundtrip`: 含中文、标题、颜色 span、无序列表、待办符号 `☐ ` 的 HTML 字符串通过 `Serializer.to_json()` / `Serializer.from_json()` 后原样保存在 `SNoteItem.content`（EDIT-01/06/07）。
    - `test_zoom_state_not_serialized`: 序列化 JSON 的 data 不包含 `zoom`、`zoom_percent`、`scale` 字段（EDIT-11，D-77）。
  </behavior>
  <action>在 `tests/model/test_serializer.py` 的 `class TestSerializerFromJson` 内追加两个 characterization 测试。富文本 HTML 测试字符串使用：`<h1>标题</h1><p><span style="color:#ff0000; background-color:#ffff00;">中文内容</span></p><ul><li>项目</li></ul><p>☐ 待办</p>`。断言 restored.content 与原字符串完全相等；此处测试的是 Serializer 纯 JSON round-trip，不是 QTextEdit 的 HTML 标准化输出。该任务不是 RED：现有 Serializer 预期应已支持字符串 round-trip，测试应作为回归契约通过。</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/model/test_serializer.py::TestSerializerFromJson::test_rich_text_html_content_roundtrip tests/model/test_serializer.py::TestSerializerFromJson::test_zoom_state_not_serialized -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/model/test_serializer.py | grep -c 'test_rich_text_html_content_roundtrip'` 输出 `1`
    - `grep -v '^#' tests/model/test_serializer.py | grep -c 'test_zoom_state_not_serialized'` 输出 `1`
    - `grep -v '^#' tests/model/test_serializer.py | grep -c '☐ 待办'` 输出 `1`
  </acceptance_criteria>
  <done>HTML 内容持久化和缩放不入模型的 characterization 测试已建立并通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试输入 → 编辑器组件契约 | 测试模拟恶意剪贴板 HTML、图片和外部 URL，防止实现遗漏安全边界。 |
| 编辑器组件 → SNoteItem.content | HTML 只应进入内存模型字段，不应引入明文临时文件或外部资源引用。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tests/ui/test_rich_text_editor.py | mitigate | RED 测试断言安全粘贴后的 HTML 不含 `<img`、`file:`、`http://`、`https://`、`<script`、`onclick=`、`onerror=`、`javascript:`。 |
| T-04-02 | Tampering | tests/ui/test_rich_text_editor.py | mitigate | RED 测试锁定待办列表为普通 `☐ ` 文本，禁止 HTML checkbox/input。 |
| T-04-03 | Information Disclosure | tests/ui/test_navigation.py | mitigate | RED 测试要求页面切换后清理 undo/redo 栈，避免上一页明文通过撤销恢复。 |
| T-04-04 | Integrity | tests/ui/test_main_window.py | mitigate | RED 测试要求缩放不修改 `note.content` 且不置脏。 |
</threat_model>

<verification>
本计划包含两个 RED UI 测试任务和一个 green characterization serializer 任务。Task 1/2 的 `<automated>` verify 用 shell snippet 包装 pytest：pytest 返回非 0 才算 RED 成功；如果测试意外通过则 verify 失败；如果输出包含 `SyntaxError` 或 `IndentationError` 也失败。Task 3 使用普通 pytest 命令，必须通过，用于锁定现有 Serializer HTML 字符串 round-trip 行为。
</verification>

<success_criteria>
- 三个测试文件包含上述精确测试函数名。
- RED 提交存在：`git log --oneline --grep="^test(04-01)" | head -1` 有输出。
- 没有修改 `src/secnotepad/` 生产代码。
</success_criteria>

<source_audit>
GOAL Phase 4 富文本编辑器：COVERED by tests for editor component, MainWindow integration, serializer persistence.
REQ EDIT-01~EDIT-11：COVERED by Task 1/2/3 RED tests.
RESEARCH Qt QTextEdit、QTextCursor、QTextCharFormat、QTextBlockFormat、QTextListFormat、安全粘贴、undo 栈清理：COVERED.
CONTEXT D-65~D-80：COVERED by tests referencing toolbar placement/state, cursor sync, color dialog, font controls, heading dropdown, alignment, todo text, zoom menu/session/status.
UI-SPEC toolbar order/copy/spacing critical text：COVERED by test expectations for labels, toolbar locality, menu names, status messages.
Deferred Ideas：None.
</source_audit>

<output>
After completion, create `.planning/phases/04-rich-text-editor/04-01-SUMMARY.md`
</output>
