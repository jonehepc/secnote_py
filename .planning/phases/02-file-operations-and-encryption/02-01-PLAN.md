---
phase: 02-file-operations-and-encryption
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/secnotepad/crypto/__init__.py
  - src/secnotepad/crypto/header.py
  - tests/crypto/__init__.py
  - tests/crypto/test_header.py
autonomous: true
requirements:
  - CRYPT-03
must_haves:
  truths:
    - "文件头可以正确组装为 69 字节的二进制数据"
    - "文件头可以从二进制数据正确解析回各字段"
    - "无效魔数被检测并拒绝"
    - "build + parse 往返测试保留所有字段"
  artifacts:
    - path: "src/secnotepad/crypto/header.py"
      provides: "文件头组装与解析"
      min_lines: 60
    - path: "tests/crypto/test_header.py"
      provides: "文件头单元测试"
      min_lines: 80
  key_links:
    - from: "crypto/header.py"
      to: "crypto/file_service.py"
      via: "import"
      pattern: "from.*header import|import.*header"
---

<objective>
实现 `.secnote` 加密文件的 69 字节二进制文件头模块 (D-23)。

**Purpose:** 提供文件头的组装（build_header）和解析（parse_header），作为加密文件服务的基础层。通过 TDD 确保二进制布局精确无误。

**Output:**
- `src/secnotepad/crypto/__init__.py` — 空包文件
- `src/secnotepad/crypto/header.py` — 文件头定义 + 组装/解析逻辑
- `tests/crypto/__init__.py` — 空包文件
- `tests/crypto/test_header.py` — 文件头单元测试
</objective>

<execution_context>
@/home/jone/projects/secnotepad/.claude/get-shit-done/workflows/execute-plan.md
@/home/jone/projects/secnotepad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-file-operations-and-encryption/02-RESEARCH.md
@.planning/phases/02-file-operations-and-encryption/02-PATTERNS.md

<interfaces>
<!-- Key contracts from existing codebase -->

From src/secnotepad/model/validator.py (pattern analog):
```python
class ValidationError(Exception):
    """SNoteItem 校验失败时抛出的异常。"""
    pass

class Validator:
    """SNoteItem 规则校验器。所有方法为静态方法，不维护状态。"""
```
</interfaces>
</context>

<feature>
  <name>加密文件头二进制布局</name>

  <files>
    - src/secnotepad/crypto/__init__.py (new)
    - src/secnotepad/crypto/header.py (new)
    - tests/crypto/__init__.py (new)
    - tests/crypto/test_header.py (new)
  </files>

  <read_first>
    - src/secnotepad/model/validator.py — 异常类模式（HeaderError 类比 ValidationError）
    - 02-PATTERNS.md — 静态方法模式、错误处理模式
    - 02-RESEARCH.md — 示例 2 的 build_header/parse_header 代码
  </read_first>

  <behavior>
    文件头按 D-23 固定布局：4B 魔数 'SN02' + 1B 版本号 (1) + 16B salt + 16B IV + 32B HMAC + 密文。总头长度 69 字节。

    **测试用例 (RED 阶段):**

    1. `test_build_header_length` — `build_header(salt, iv, hmac_tag)` 返回 bytes，长度 == 69
    2. `test_build_header_magic` — 前 4 字节为 b'SN02'
    3. `test_build_header_version` — 第 5 字节为 1
    4. `test_parse_header_roundtrip` — build → parse 返回相同 salt, iv, hmac_tag
    5. `test_parse_header_ciphertext` — parse 正确提取头后的密文部分
    6. `test_parse_header_invalid_magic` — 魔数不匹配时抛出 HeaderError
    7. `test_parse_header_short_data` — 数据少于 69 字节时抛出 HeaderError
    8. `test_parse_header_unknown_version` — 版本号 > 1 时不拒绝（向前兼容），返回实际版本号
    9. `test_header_constants` — HEADER_SIZE == 69, MAGIC == b'SN02', VERSION == 1

    **输入/输出:**

    ```python
    salt = b'\x00' * 16
    iv = b'\x01' * 16
    hmac_tag = b'\x02' * 32
    header = build_header(salt, iv, hmac_tag)
    # assert len(header) == 69
    # assert header[:4] == b'SN02'

    parsed = parse_header(header + b'ciphertext_data')
    # assert parsed['salt'] == salt
    # assert parsed['iv'] == iv
    # assert parsed['hmac_tag'] == hmac_tag
    # assert parsed['ciphertext'] == b'ciphertext_data'

    parse_header(b'BAD\x00' + b'\x00' * 65)
    # raises HeaderError: "无效的魔数"
    ```
  </behavior>

  <implementation>
    **包文件:**
    - `crypto/__init__.py` — 空文件（仅注释 `# crypto package`）
    - `tests/crypto/__init__.py` — 空文件（仅注释 `# crypto tests`）

    **header.py 实现 (GREEN 阶段):**

    遵循 PATTERNS.md 中 validator.py 的 "stateless utility + custom exception" 模式：

    ```python
    """.secnote 加密文件头定义与组装/解析 (D-23)"""

    import struct

    MAGIC = b'SN02'
    VERSION = 1
    HEADER_SIZE = 69  # 4 + 1 + 16 + 16 + 32


    class HeaderError(Exception):
        """文件头解析失败时抛出的异常。"""
        pass


    class Header:
        """文件头组装与解析。所有方法为静态方法，不维护状态。"""

        @staticmethod
        def build(salt: bytes, iv: bytes, hmac_tag: bytes) -> bytes:
            """组装 69 字节文件头。"""
            assert len(salt) == 16, "salt must be 16 bytes"
            assert len(iv) == 16, "IV must be 16 bytes"
            assert len(hmac_tag) == 32, "HMAC tag must be 32 bytes"
            return struct.pack(
                '!4s B 16s 16s 32s',
                MAGIC, VERSION, salt, iv, hmac_tag,
            )

        @staticmethod
        def parse(data: bytes) -> dict:
            """解析文件头，返回各部分字典。

            Returns:
                dict with keys: version, salt, iv, hmac_tag, ciphertext

            Raises:
                HeaderError: 魔数无效或数据长度不足。
            """
            if len(data) < HEADER_SIZE:
                raise HeaderError(
                    f"数据过短: {len(data)} 字节, 需要至少 {HEADER_SIZE}"
                )
            magic, version, salt, iv, hmac_tag = struct.unpack(
                '!4s B 16s 16s 32s', data[:HEADER_SIZE]
            )
            if magic != MAGIC:
                raise HeaderError(f"无效的魔数: {magic!r}")
            return {
                'version': version,
                'salt': salt,
                'iv': iv,
                'hmac_tag': hmac_tag,
                'ciphertext': data[HEADER_SIZE:],
            }
    ```

    **GREEN 关键约束:**
    - 使用 `struct.pack`/`unpack` 和 `!`（network byte order / big-endian）
    - 不引入 cryptography 依赖（header 只做二进制布局，纯标准库）
    - 魔数校验是唯一的安全检查（版本号向前兼容）

    **REFACTOR (可选):**
    - 如果 build 和 parse 逻辑对称，考虑提取常量或辅助函数
    - 验证 docstring 是否符合 project 标准（Args/Returns/Raises 格式）
  </implementation>

  <verification>
    <automated>python -m pytest tests/crypto/test_header.py -x -q</automated>
  </verification>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘文件 → 应用 | 文件头从不可信的磁盘文件读取，解析时可能遇到篡改数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S (Spoofing) | `Header.parse()` | mitigate | 魔数校验 (MAGIC == b'SN02') 拒绝非 .secnote 文件 |
| T-02-02 | T (Tampering) | `Header.parse()` | mitigate | 固定 69 字节解析，struct.unpack 自动验证长度 |
| T-02-03 | I (Info Disclosure) | `HeaderError` 消息 | mitigate | 不泄露文件名或路径，仅报告"无效的魔数" |
</threat_model>

<verification>
```bash
python -m pytest tests/crypto/test_header.py -x -q
# 应全部通过
```
</verification>

<success_criteria>
- RED 阶段：测试文件创建，运行 pytest 全部失败（因为 header.py 不存在或为空）
- GREEN 阶段：header.py 实现完成后，pytest 全部通过
- REFACTOR 阶段（可选）：代码清理后测试仍通过
- 符合 RED→GREEN→REFACTOR 三次提交模式
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-operations-and-encryption/02-01-SUMMARY.md`
</output>
