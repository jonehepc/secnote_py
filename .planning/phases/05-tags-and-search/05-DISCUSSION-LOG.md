# Phase 05: 标签与搜索 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 05-tags-and-search
**Areas discussed:** 标签入口与展示, 标签命名规则, 搜索入口与范围, 结果片段与跳转

---

## 标签入口与展示

| Question | Options | Selected |
|----------|---------|----------|
| 标签编辑入口放在哪里最符合你的预期？ | 编辑区顶部 / 页面列表内 / 两处都有 | 编辑区顶部 |
| 标签在编辑区顶部应该用哪种视觉和交互形式？ | Chip 行 / 单行输入框 / 弹窗编辑 | Chip 行 |
| 页面列表中是否也要显示标签？ | 暂不显示 / 标题下显示 / 仅图标提示 | 暂不显示 |
| 没有选中页面时，标签区域应该怎么表现？ | 随编辑器禁用 / 显示空状态 / 完全隐藏 | 随编辑器禁用 |

**User's choice:** 编辑区顶部 chip 行；页面列表暂不显示；无选中页面时标签区域随编辑器禁用。  
**Notes:** 该选择保持 PageListModel 单列标题模式，标签作为当前页面元数据在右侧编辑区管理。

---

## 标签命名规则

| Question | Options | Selected |
|----------|---------|----------|
| 标签名称应该如何规范化？ | 去空格保原样 / 全部小写 / 严格 slug | 去空格保原样 |
| 重复标签如何判断？ | 忽略大小写 / 严格区分 / 完全规范化 | 忽略大小写 |
| 添加标签时是否需要复用已有标签？ | 下拉补全 / 自由输入 / 只能选已有 | 下拉补全 |
| 空标签、超长标签或特殊符号应该怎么处理？ | 轻量限制 / 严格限制 / 不额外限制 | 轻量限制 |

**User's choice:** trim 后保留原样显示，重复判断忽略大小写，添加时下拉补全已有标签，空值与超长标签做轻量限制。  
**Notes:** 允许中文、空格和常见符号；不做严格 slug 化。

---

## 搜索入口与范围

| Question | Options | Selected |
|----------|---------|----------|
| 搜索入口应该放在哪里？ | 中间栏顶部 / 主工具栏 / 菜单弹窗 | 菜单弹窗 |
| 搜索触发方式你更偏向哪一种？ | 回车搜索 / 实时搜索 / 两者结合 | 回车搜索 |
| 搜索范围默认应该覆盖哪里？ | 全部笔记本 / 当前分区 / 可选范围 | 全部笔记本 |
| 搜索应该匹配哪些字段？ | 标题和正文 / 含标签 / 字段可筛选 | 字段可筛选 |

**User's choice:** 搜索通过菜单弹窗进入，回车或按钮触发，默认覆盖全部笔记本，并提供标题、正文、标签字段筛选。  
**Notes:** 字段筛选将标签搜索纳入 Phase 5，但仍属于标签与搜索边界内。

---

## 结果片段与跳转

| Question | Options | Selected |
|----------|---------|----------|
| 搜索结果列表应该显示哪些信息？ | 标题+路径+片段 / 标题+片段 / 标题+标签+片段 | 标题+路径+片段 |
| HTML 正文的匹配片段应该如何生成？ | 纯文本片段 / 保留富文本 / 只显示命中字段 | 纯文本片段 |
| 点击搜索结果后应该跳转到什么程度？ | 选中页面 / 定位到匹配处 / 打开只读预览 | 选中页面 |
| 搜索结果弹窗在跳转后应该如何处理？ | 保持打开 / 自动关闭 / 用户可固定 | 保持打开 |

**User's choice:** 结果显示标题、分区路径和纯文本高亮片段；点击后选中对应页面；搜索弹窗保持打开。  
**Notes:** 不要求 QTextEdit 光标定位到正文匹配位置。

---

## Claude's Discretion

无。

## Deferred Ideas

无。
