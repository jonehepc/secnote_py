---
phase: 04-rich-text-editor
plan: 06
type: execute
wave: 6
depends_on: [04-05]
files_modified:
  - tests/model/test_serializer.py
  - tests/ui/test_rich_text_editor.py
  - tests/ui/test_main_window.py
  - tests/ui/test_navigation.py
  - src/secnotepad/ui/rich_text_editor.py
  - src/secnotepad/ui/main_window.py
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10, EDIT-11]
must_haves:
  truths:
    - "富文本 HTML 内容可通过 Serializer JSON round-trip 原样保存"
    - "缩放状态不出现在序列化 JSON 中"
    - "Phase 04 的组件、MainWindow、导航和模型测试形成完整回归门"
  artifacts:
    - path: "tests/model/test_serializer.py"
      provides: "富文本 HTML round-trip 和缩放不序列化测试"
      contains: "test_rich_text_html_content_roundtrip"
    - path: "tests/ui/test_rich_text_editor.py"
      provides: "富文本组件完整回归测试"
      contains: "test_clipboard_actions_and_safe_paste"
    - path: "tests/ui/test_main_window.py"
      provides: "MainWindow 富文本集成回归测试"
      contains: "TestRichTextIntegration"
  key_links:
    - from: "SNoteItem.content"
      to: "Serializer.to_json/from_json"
      via: "dataclass asdict JSON round-trip"
      pattern: "content"
    - from: "Phase 04 test suite"
      to: "all EDIT requirements"
      via: "pytest commands in validation strategy"
      pattern: "test_rich_text_editor"
---

<objective>
补齐富文本 HTML 持久化测试并执行 Phase 04 回归门，确保 EDIT-01~EDIT-11、D-65~D-80、安全要求和 UI 契约全部可验证。

Purpose: 在实现完成后用模型 round-trip 与全量相关 UI 测试防止遗漏，尤其防止缩放状态进入加密文件或 HTML 粘贴引入外部资源。
Output: 持久化测试通过、Phase 04 相关测试套件通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rich-text-editor/04-CONTEXT.md
@.planning/phases/04-rich-text-editor/04-RESEARCH.md
@.planning/phases/04-rich-text-editor/04-PATTERNS.md
@.planning/phases/04-rich-text-editor/04-VALIDATION.md
@.planning/phases/04-rich-text-editor/04-UI-SPEC.md
@src/secnotepad/model/snote_item.py
@src/secnotepad/model/serializer.py
@src/secnotepad/ui/rich_text_editor.py
@src/secnotepad/ui/main_window.py
@tests/model/test_serializer.py
@tests/ui/test_rich_text_editor.py
@tests/ui/test_main_window.py
@tests/ui/test_navigation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现富文本 HTML 持久化测试并确认模型无需改动</name>
  <files>tests/model/test_serializer.py</files>
  <read_first>
    - tests/model/test_serializer.py
    - src/secnotepad/model/snote_item.py
    - src/secnotepad/model/serializer.py
    - .planning/phases/04-rich-text-editor/04-CONTEXT.md
    - .planning/phases/04-rich-text-editor/04-VALIDATION.md
  </read_first>
  <behavior>
    - `test_rich_text_html_content_roundtrip` 通过：`<h1>标题</h1><p><span style="color:#ff0000; background-color:#ffff00;">中文内容</span></p><ul><li>项目</li></ul><p>☐ 待办</p>` 经 Serializer round-trip 后原样保存在 restored.content（EDIT-01/06/07）。
    - `test_zoom_state_not_serialized` 通过：Serializer JSON data 不包含 `zoom`、`zoom_percent`、`scale` 任一字段（EDIT-11，D-77）。
    - 不修改 `src/secnotepad/model/snote_item.py`，确认 SNoteItem 字段仍为 `content: str = ""` 且没有 zoom 字段。
  </behavior>
  <action>如果 04-01 已添加测试但仍失败，修正测试或 Serializer 现有行为使其通过。优先不改生产模型代码；Serializer 当前基于 dataclass `asdict(root)` 应天然保留 content 字符串。若发现生产代码已有 zoom/scale 字段，删除该字段及序列化路径，因为 D-77 锁定缩放不写入页面 HTML、不写入 `.secnote`、不新增 SNoteItem 字段。不要改加密 FileService，本阶段只验证 JSON 层富文本内容。</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/model/test_serializer.py::TestSerializerFromJson::test_rich_text_html_content_roundtrip tests/model/test_serializer.py::TestSerializerFromJson::test_zoom_state_not_serialized -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/model/test_serializer.py | grep -c 'test_rich_text_html_content_roundtrip'` 输出 `1`
    - `grep -v '^#' tests/model/test_serializer.py | grep -c 'test_zoom_state_not_serialized'` 输出 `1`
    - `grep -v '^#' src/secnotepad/model/snote_item.py | grep -c 'zoom'` 输出 `0`
    - `grep -v '^#' src/secnotepad/model/snote_item.py | grep -c 'content: str = ""'` 输出 `1`
  </acceptance_criteria>
  <done>富文本 HTML JSON round-trip 通过，缩放状态未进入模型或序列化输出。</done>
</task>

<task type="auto">
  <name>Task 2: 执行 Phase 04 相关回归并修复测试契约偏差</name>
  <files>tests/ui/test_rich_text_editor.py, tests/ui/test_main_window.py, tests/ui/test_navigation.py, tests/model/test_serializer.py, src/secnotepad/ui/rich_text_editor.py, src/secnotepad/ui/main_window.py</files>
  <read_first>
    - tests/ui/test_rich_text_editor.py
    - tests/ui/test_main_window.py
    - tests/ui/test_navigation.py
    - tests/model/test_serializer.py
    - src/secnotepad/ui/rich_text_editor.py
    - src/secnotepad/ui/main_window.py
    - .planning/phases/04-rich-text-editor/04-VALIDATION.md
  </read_first>
  <action>运行 Phase 04 相关回归命令：`QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_rich_text_editor.py tests/ui/test_main_window.py tests/ui/test_navigation.py tests/model/test_serializer.py -q`。若失败原因是测试仍断言 Phase 3 旧状态（例如 `test_edit_actions_disabled` 要求编辑菜单永远灰显），将旧测试更新为 Phase 04 目标：初始无页面时编辑 action disabled，选中页面后按文档/剪贴板状态启用。若失败原因是实现未满足 04-CONTEXT / 04-UI-SPEC，修生产代码而非放松测试。保持所有测试函数名不变，避免破坏验证映射。</action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_rich_text_editor.py tests/ui/test_main_window.py tests/ui/test_navigation.py tests/model/test_serializer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 回归命令退出码为 `0`
    - `grep -v '^#' tests/ui/test_main_window.py | grep -c 'test_edit_actions_disabled'` 输出 `1`，且该测试断言无页面初始状态而非永久灰显
    - `grep -v '^#' tests/ui/test_rich_text_editor.py | grep -c 'test_clipboard_actions_and_safe_paste'` 输出 `1`
    - `grep -v '^#' tests/ui/test_navigation.py | grep -c 'test_switching_pages_clears_undo_stack_and_does_not_mark_dirty'` 输出 `1`
  </acceptance_criteria>
  <done>Phase 04 相关 UI/model 测试全部通过，旧测试已按新需求更新而非删除。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证富文本编辑器核心交互</name>
  <files>src/secnotepad/ui/rich_text_editor.py, src/secnotepad/ui/main_window.py</files>
  <read_first>
    - .planning/phases/04-rich-text-editor/04-UI-SPEC.md
    - .planning/phases/04-rich-text-editor/04-CONTEXT.md
    - src/secnotepad/ui/rich_text_editor.py
    - src/secnotepad/ui/main_window.py
  </read_first>
  <action>暂停自动执行并让用户按 how-to-verify 步骤检查真实桌面交互；收到 `approved` 后继续总结，收到失败现象后修复对应 D-65~D-80 或 EDIT-01~EDIT-11 缺口并重新运行 Task 2 自动回归。</action>
  <what-built>Claude 已自动实现并测试富文本编辑器、格式工具栏、编辑菜单、安全粘贴和缩放。</what-built>
  <how-to-verify>
    1. 使用 `.venv/bin/python -m secnotepad` 启动应用。
    2. 新建笔记本，新建分区和页面。
    3. 确认右侧编辑区上方显示格式工具栏；未选中页面时工具栏禁用，选中页面后启用。
    4. 输入中文内容，依次测试加粗、斜体、下划线、删除线、字体、字号、文字颜色、背景色。
    5. 测试段落样式下拉 `正文/H1/H2/H3/H4/H5/H6`、左/中/右/两端对齐、无序列表、有序列表、待办列表 `☐ `、增加/减少缩进。
    6. 测试编辑菜单撤销、重做、剪切、复制、粘贴。
    7. 测试视图菜单或快捷键 Ctrl+Plus、Ctrl+Minus、Ctrl+0；确认状态栏显示 `缩放：{percent}%`，切换页面后缩放保持，重置后显示 `缩放：100%`。
    8. 保存为 `.secnote` 后不要在普通文本编辑器中看到笔记 HTML 明文；重新打开后内容可恢复。
  </how-to-verify>
  <acceptance_criteria>
    - 人工回复包含 `approved` 表示通过，或列出具体失败项。
    - 若失败项涉及 D-65~D-80 任一锁定决策，必须修复后重新运行 Task 2 自动回归。
  </acceptance_criteria>
  <resume-signal>输入 `approved` 或描述失败现象。</resume-signal>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_rich_text_editor.py tests/ui/test_main_window.py tests/ui/test_navigation.py tests/model/test_serializer.py -q</automated>
  </verify>
  <done>人工确认富文本编辑器满足 UI-SPEC 和用户锁定决策。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serializer JSON → encrypted FileService | 富文本 HTML 进入 JSON 后必须继续通过既有加密保存链路，不新增明文缓存。 |
| Automated tests → release confidence | 自动测试必须覆盖安全粘贴、缩放不持久化、undo 栈清理，避免人工遗漏。 |
| Human GUI verification → final acceptance | 颜色对话框和真实桌面快捷键需要人工确认。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Information Disclosure | Serializer rich HTML | mitigate | 测试确认 HTML 只在 `content` 字段 round-trip；保存仍走既有 `.secnote` 加密链路。 |
| T-04-22 | Information Disclosure | zoom state | mitigate | grep/test 确认 SNoteItem 无 zoom 字段，JSON data 无 zoom/scale。 |
| T-04-23 | Tampering | legacy tests | mitigate | 旧测试只能更新为 Phase 04 目标，禁止删除关键测试函数逃避验证。 |
| T-04-24 | Repudiation | manual GUI verification | accept | 本地单用户应用不做审计；人工验收记录在 SUMMARY 中。 |
</threat_model>

<verification>
- `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ui/test_rich_text_editor.py tests/ui/test_main_window.py tests/ui/test_navigation.py tests/model/test_serializer.py -q`
- Phase gate: `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest -q`
</verification>

<success_criteria>
- 富文本 HTML round-trip 测试通过。
- Phase 04 相关回归全部通过。
- 人工验证通过或明确记录待修复问题。
- D-65~D-80 全部有实现和测试覆盖。
</success_criteria>

<source_audit>
GOAL Phase 4 完整富文本编辑器：COVERED by final regression and human verification.
REQ EDIT-01~EDIT-11：COVERED by all phase tests.
RESEARCH validation architecture, security domain, pitfalls：COVERED.
CONTEXT D-65~D-80：COVERED.
UI-SPEC manual-only color dialog/status bar checks：COVERED via checkpoint.
Deferred Ideas：None.
</source_audit>

<output>
After completion, create `.planning/phases/04-rich-text-editor/04-06-SUMMARY.md`
</output>
