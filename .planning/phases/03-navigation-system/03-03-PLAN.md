---
phase: 03-navigation-system
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/secnotepad/model/tree_model.py
  - src/secnotepad/ui/main_window.py
  - tests/model/test_tree_model.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "打开/新建笔记本后，左侧 QTreeView 通过 SectionFilterProxy 仅显示分区节点"
    - "中间 QListView 通过 PageListModel 显示当前选中分区下的页面列表"
    - "点击分区树节点时，页面列表自动切换为该分区下的页面"
    - "分区树打开后仅展开第一层，自动选中根分区下第一个子分区 (D-52, D-53)"
    - "右侧编辑区在无页面选中时显示居中提示文字'请在页面列表中选择一个页面' (D-63)"
    - "点击页面后编辑区显示该页面的只读 HTML 内容预览 (D-62)"
    - "分区节点支持原地重命名（双击或 F2），空名称被拒绝 (D-59, D-60)"
  artifacts:
    - path: "src/secnotepad/model/tree_model.py"
      provides: "TreeModel.setData(EditRole) + flags 包含 ItemIsEditable"
      contains: "def setData"
    - path: "tests/model/test_tree_model.py"
      provides: "TreeModel.setData 行为测试"
      contains: "class TestTreeModelSetData"
    - path: "src/secnotepad/ui/main_window.py"
      provides: "导航系统集成: _setup_editor_area(), _setup_navigation(), _initialize_navigation_state(), 信号连接"
      contains: "def _setup_navigation"
  key_links:
    - from: "_setup_navigation() → SectionFilterProxy.setSourceModel"
      to: "self._tree_model (TreeModel)"
      via: "self._section_filter.setSourceModel(self._tree_model)"
      pattern: "setSourceModel.*_tree_model"
    - from: "_setup_navigation() → QTreeView.setModel"
      to: "self._section_filter (SectionFilterProxy)"
      via: "self._tree_view.setModel(self._section_filter)"
      pattern: "_tree_view\\.setModel.*_section_filter"
    - from: "_setup_navigation() → QListView.setModel"
      to: "self._page_list_model (PageListModel)"
      via: "self._list_view.setModel(self._page_list_model)"
      pattern: "_list_view\\.setModel.*_page_list_model"
    - from: "_on_tree_current_changed → PageListModel.set_section"
      to: "SNoteItem(section) from proxy.mapToSource(current).internalPointer()"
      via: "self._page_list_model.set_section(section_item)"
      pattern: "_page_list_model\\.set_section"
    - from: "_on_page_current_changed → QTextEdit.setHtml"
      to: "SNoteItem(note).content from PageListModel.note_at(current)"
      via: "self._editor_preview.setHtml(note.content or '')"
      pattern: "_editor_preview\\.setHtml"
---

<objective>
增强 TreeModel 支持原地重命名（setData EditRole），并在 MainWindow 中搭建完整导航系统：创建 SectionFilterProxy + PageListModel + 只读编辑预览区，连接选择信号，设置初始导航状态。

Purpose: 将 Plan 01 和 Plan 02 产生的模型组件（SectionFilterProxy, PageListModel）集成到 MainWindow 中，实现分区树过滤 → 页面列表联动的核心导航逻辑。这是 Phase 03 的"骨架"——后续 Plan 04 在此基础上添加 CRUD 操作和用户交互。
Output: TreeModel 支持可编辑标志；MainWindow 中三栏导航完整运转（树选区分 → 列表显示页面 → 编辑区预览内容）。
</objective>

<execution_context>
@/home/jone/projects/secnotepad/.claude/get-shit-done/workflows/execute-plan.md
@/home/jone/projects/secnotepad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-navigation-system/03-CONTEXT.md
@.planning/phases/03-navigation-system/03-RESEARCH.md
@.planning/phases/03-navigation-system/03-PATTERNS.md
@.planning/phases/03-navigation-system/03-VALIDATION.md
@src/secnotepad/model/tree_model.py
@src/secnotepad/model/snote_item.py
@src/secnotepad/ui/main_window.py
@tests/conftest.py
@tests/model/test_tree_model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 TreeModel.setData(EditRole) 和 ItemIsEditable 标志</name>
  <files>
    src/secnotepad/model/tree_model.py
    tests/model/test_tree_model.py
  </files>
  <read_first>
    src/secnotepad/model/tree_model.py (当前状态 — 了解现有 flags/data 方法签名)
    tests/model/test_tree_model.py (测试 fixture 和类结构 — 定位插入位置)
  </read_first>
  <behavior>
    - Test 1: setData(valid_index, "新名称", Qt.EditRole) → True, data(DisplayRole) 返回 "新名称", SNoteItem.title == "新名称"
    - Test 2: setData(valid_index, "", Qt.EditRole) → False, title 不变 (D-60)
    - Test 3: setData(valid_index, "   ", Qt.EditRole) → False, title 不变 (D-60 纯空格)
    - Test 4: setData(valid_index, "test", Qt.DisplayRole) → False (非 EditRole)
    - Test 5: setData(QModelIndex(), "test", Qt.EditRole) → False (无效索引)
    - Test 6: flags(valid_index) → 包含 Qt.ItemIsEditable (新增)
  </behavior>
  <action>
    **A. 修改 tree_model.py — flags() 方法 (line 103-106):**

    将当前返回值从:
    ```python
    return Qt.ItemIsEnabled | Qt.ItemIsSelectable
    ```
    改为:
    ```python
    return Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsEditable
    ```
    这将允许 QTreeView 对分区节点触发编辑事件 (D-59)。代码缩进保持在 flags() 方法内。

    **B. 修改 tree_model.py — data() 方法 (line 88-101):**

    在 data() 方法中，将 `if role == Qt.DisplayRole:` 改为 `if role in (Qt.DisplayRole, Qt.EditRole):`。
    即 line 99 改为:
    ```python
            if role in (Qt.DisplayRole, Qt.EditRole):
    ```
    这确保 EditRole 从 DisplayRole 回退获取当前值（Qt 的 `setEditTriggers` 依赖 EditRole 获取编辑前的值）。

    **C. 向 tree_model.py 添加 setData() 方法:**

    在 "数据变更接口" 区块内（line 145 之后，add_item/remove_item 之后）插入:

    ```python
        def setData(self, index: QModelIndex, value,
                    role: int = Qt.EditRole) -> bool:
            """重命名分区标题 (D-59, D-60: 拒绝空名称)。

            Phase 3 添加，支持原地编辑 (EditRole)。
            修改 SNoteItem.title 并触发 dataChanged 信号使视图更新。
            """
            if role != Qt.EditRole:
                return False
            if not index.isValid():
                return False
            # D-60: 拒绝空名称和纯空格
            if not isinstance(value, str) or value.strip() == "":
                return False
            item: SNoteItem = index.internalPointer()
            item.title = value.strip()
            self.dataChanged.emit(index, index, [Qt.DisplayRole, Qt.EditRole])
            return True
    ```

    **D. 向 test_tree_model.py 添加测试类:**

    在文件末尾（最后一个已有测试类之后）添加 `TestTreeModelSetData` 类，包含上述 6 个测试方法。使用 conftest.py 中的 `sample_tree` fixture 或定义自己的 fixture。遵循已有测试类的 pytest 风格（类内方法以 `test_` 前缀，不使用 `unittest.TestCase`）。

    验证命令: `python -m pytest tests/model/test_tree_model.py -x -k "setdata" --timeout=10`
  </action>
  <acceptance_criteria>
    - `grep -c "def setData" src/secnotepad/model/tree_model.py` 输出 1
    - `grep -c "ItemIsEditable" src/secnotepad/model/tree_model.py` 输出 1
    - `grep -c "Qt.DisplayRole, Qt.EditRole" src/secnotepad/model/tree_model.py` 输出 1
    - `grep -c "not isinstance(value, str) or value.strip() == \"\"" src/secnotepad/model/tree_model.py` 输出 1
    - `grep -c "class TestTreeModelSetData" tests/model/test_tree_model.py` 输出 1
    - `python -m pytest tests/model/test_tree_model.py -x -k "setdata" --timeout=10` 6 个测试全部通过
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/model/test_tree_model.py -x -k "setdata" --timeout=10</automated>
  </verify>
  <done>
    TreeModel.setData() 实现完毕，flags() 包含 ItemIsEditable，所有 setData 测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 搭建导航系统 — 编辑区 + 模型绑定 + 信号连接</name>
  <files>
    src/secnotepad/ui/main_window.py
  </files>
  <read_first>
    src/secnotepad/ui/main_window.py (整文件，重点: __init__ (lines 26-46), _setup_central_area (lines 134-170), _on_new_notebook (lines 292-331), _on_open_notebook (lines 333-375), _on_open_recent (lines 534-571), _connect_actions (lines 277-290))
    src/secnotepad/model/section_filter_proxy.py (Plan 01 产出 — 了解 SectionFilterProxy API)
    src/secnotepad/model/page_list_model.py (Plan 02 产出 — 了解 PageListModel API)
  </read_first>
  <action>
    以下所有修改在 `src/secnotepad/ui/main_window.py` 中进行。

    **A. 导入语句（文件顶部 import 块）:**

    添加以下新导入。不要动已有的 `QMainWindow, QWidget, QSplitter, QTreeView, QListView, QStackedWidget, QStyle, QVBoxLayout, QMessageBox, QFileDialog, QDialog` 导入 — QTextEdit, QLabel, QAbstractItemView 是新增类型，需要加入现有导入语句。

    a) 在 `from PySide6.QtWidgets import (...)` 的圆括号内，在 `QDialog` 后面添加 `, QTextEdit, QLabel, QAbstractItemView`:

    ```python
    from PySide6.QtWidgets import (QMainWindow, QWidget, QSplitter,
                                    QTreeView, QListView, QStackedWidget,
                                    QStyle, QVBoxLayout, QMessageBox,
                                    QFileDialog, QDialog,
                                    QTextEdit, QLabel, QAbstractItemView)
    ```

    b) 在现有模型导入下面（`from ..model.tree_model import TreeModel` 之后），添加:

    ```python
    from ..model.section_filter_proxy import SectionFilterProxy
    from ..model.page_list_model import PageListModel
    ```

    **B. __init__ 方法 (line 26-46) — 添加导航实例变量:**

    在 `self._tree_model: TreeModel = None` (line 30) 之后，`# ── 文件操作状态 (Phase 2) ──` 注释之前，插入:

    ```python

            # ── 导航系统状态 (Phase 3) ──
            self._section_filter: SectionFilterProxy = None
            self._page_list_model: PageListModel = None
            self._editor_preview: QTextEdit = None
            self._editor_stack: QStackedWidget = None
            self._editor_placeholder_label: QLabel = None
    ```

    注意缩进：8 个空格（MainWindow.__init__ 方法体内）。

    **C. _setup_central_area (line 134-170) — 替换 _editor_placeholder:**

    将 line 160-161:
    ```python
            self._editor_placeholder = QWidget()
            splitter.addWidget(self._editor_placeholder)  # 右侧
    ```
    替换为:
    ```python
            self._setup_editor_area()
            splitter.addWidget(self._editor_stack)         # 右侧
    ```

    **D. 添加 _setup_editor_area 方法:**

    在 `_setup_central_area` 方法之后（line 170 后空行之后）添加完整新方法:

    ```python
        def _setup_editor_area(self):
            """创建右侧编辑区: 只读预览 + placeholder 堆叠 (D-62, D-63)。

            使用 QStackedWidget 切换两种状态:
              index 0 = QTextEdit (只读 HTML 预览)
              index 1 = QLabel ("请在页面列表中选择一个页面")
            默认为 placeholder (index 1)。
            """
            self._editor_preview = QTextEdit()
            self._editor_preview.setReadOnly(True)

            self._editor_placeholder_label = QLabel(
                "请在页面列表中选择一个页面"
            )
            self._editor_placeholder_label.setAlignment(Qt.AlignCenter)
            self._editor_placeholder_label.setStyleSheet(
                "color: #888; font-size: 14px;"
            )

            self._editor_stack = QStackedWidget()
            self._editor_stack.addWidget(self._editor_preview)          # index 0
            self._editor_stack.addWidget(self._editor_placeholder_label)  # index 1
            self._editor_stack.setCurrentIndex(1)  # 默认显示 placeholder
    ```

    注意缩进：4 个空格（MainWindow 类的方法）。

    **E. 添加 _setup_navigation 方法:**

    在 `_setup_editor_area` 方法之后添加:

    ```python
        def _setup_navigation(self):
            """Phase 3: 设置导航系统 — 代理模型、页面列表、信号连接、初始状态。

            必须在 _tree_model 存在且 _tree_view / _list_view 可见之后调用。
            即在 _on_new_notebook / _on_open_notebook / _on_open_recent 中
            设置 TreeModel 后调用。

            实现 D-49~D-54, D-59, D-62, D-63。
            """
            # --- 1. SectionFilterProxy: 过滤分区树仅显示 section (D-49) ---
            self._section_filter = SectionFilterProxy(self)
            self._section_filter.setSourceModel(self._tree_model)
            self._tree_view.setModel(self._section_filter)

            # D-59: 原地编辑 — 必须在 setModel 之后设置 (Pitfall 2)
            self._tree_view.setEditTriggers(
                QAbstractItemView.DoubleClicked | QAbstractItemView.EditKeyPressed
            )

            # --- 2. PageListModel: 平铺显示页面的列表 (D-50, D-54) ---
            self._page_list_model = PageListModel(self)
            self._list_view.setModel(self._page_list_model)

            # D-59: 原地编辑 — 必须在 setModel 之后设置 (Pitfall 2)
            self._list_view.setEditTriggers(
                QAbstractItemView.DoubleClicked | QAbstractItemView.EditKeyPressed
            )

            # --- 3. 选择信号连接 (D-51, D-62, Pitfall 3) ---
            # 必须在 setModel 之后获取 selectionModel (Pitfall 3)
            self._tree_selection = self._tree_view.selectionModel()
            self._tree_selection.currentChanged.connect(
                self._on_tree_current_changed
            )

            self._page_selection = self._list_view.selectionModel()
            self._page_selection.currentChanged.connect(
                self._on_page_current_changed
            )

            # --- 4. 初始导航状态 (D-52, D-53) ---
            self._initialize_navigation_state()
    ```

    **F. 添加 _on_tree_current_changed / _on_page_current_changed / _show_editor_placeholder 方法:**

    ```python
        def _on_tree_current_changed(self, current: QModelIndex,
                                     previous: QModelIndex):
            """分区选择变化 → 更新页面列表 (D-51)。

            从 proxy 索引映射回源模型索引，获取 SNoteItem(section)，
            调用 PageListModel.set_section() 刷新列表。
            每次分区切换时重置编辑区为 placeholder (D-63)。
            """
            source_index = self._section_filter.mapToSource(current)
            if source_index.isValid():
                section_item = source_index.internalPointer()
                self._page_list_model.set_section(section_item)
            else:
                self._page_list_model.set_section(None)
            self._show_editor_placeholder()

        def _on_page_current_changed(self, current: QModelIndex,
                                     previous: QModelIndex):
            """页面选择变化 → 显示只读预览 (D-62, D-63)。

            获取 SNoteItem(note) 后调用 QTextEdit.setHtml() 显示内容。
            无效选择（取消选择）时显示 placeholder。
            """
            note = self._page_list_model.note_at(current)
            if note is not None:
                self._editor_preview.setHtml(note.content or "")
                self._editor_stack.setCurrentIndex(0)
            else:
                self._show_editor_placeholder()

        def _show_editor_placeholder(self):
            """显示 placeholder 提示文字 (D-63)。"""
            self._editor_preview.clear()
            self._editor_stack.setCurrentIndex(1)
    ```

    **G. 添加 _initialize_navigation_state 方法:**

    ```python
        def _initialize_navigation_state(self):
            """打开笔记本后展开分区树第一层并自动选中第一个子分区 (D-52, D-53)。

            遍历代理模型的根级行，仅 expand(index) — 不递归展开 (D-53)。
            然后选中根节点的第一个子分区 (D-52)。
            """
            proxy = self._section_filter
            # D-53: 仅展开第一层（根节点的直接子节点）
            for row in range(proxy.rowCount()):
                index = proxy.index(row, 0, QModelIndex())
                self._tree_view.expand(index)

            # D-52: 自动选中第一个子分区
            if proxy.rowCount() > 0:
                first_index = proxy.index(0, 0, QModelIndex())
                if first_index.isValid():
                    self._tree_view.setCurrentIndex(first_index)
    ```
    注意：`QModelIndex` 来自 `PySide6.QtCore`，已在文件顶部导入（line 10: `from PySide6.QtCore import Qt`）。需要扩展此导入为 `from PySide6.QtCore import Qt, QModelIndex`。

    **H. 修改 _on_new_notebook (line 313) — 调用 _setup_navigation:**

    在 line 315 `self._tree_view.setModel(self._tree_model)` 的下一行，
    将:
    ```python
            self._stack.setCurrentIndex(1)               # 切换到三栏布局
    ```
    替换为:
    ```python
            self._setup_navigation()                     # Phase 3: 导航系统
            self._stack.setCurrentIndex(1)               # 切换到三栏布局
    ```

    **I. 修改 _on_open_notebook (line 359) — 调用 _setup_navigation:**

    在 line 359 `self._tree_view.setModel(self._tree_model)` 的下一行插入:
    ```python
            self._setup_navigation()                     # Phase 3: 导航系统
    ```

    **J. 修改 _on_open_recent (line 557) — 调用 _setup_navigation:**

    在 line 557 `self._tree_view.setModel(self._tree_model)` 的下一行插入:
    ```python
            self._setup_navigation()                     # Phase 3: 导航系统
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "from ..model.section_filter_proxy import SectionFilterProxy" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "from ..model.page_list_model import PageListModel" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _setup_navigation" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _setup_editor_area" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _on_tree_current_changed" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _on_page_current_changed" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _show_editor_placeholder" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "def _initialize_navigation_state" src/secnotepad/ui/main_window.py` 输出 1
    - `grep -c "SectionFilterProxy" src/secnotepad/ui/main_window.py` 至少输出 2（import + 初始化）
    - `grep -c "PageListModel" src/secnotepad/ui/main_window.py` 至少输出 2（import + 初始化）
    - `grep -c "_setup_navigation()" src/secnotepad/ui/main_window.py` 输出 3（_on_new, _on_open, _on_open_recent 各一处调用）
    - `grep -c "self\._editor_placeholder" src/secnotepad/ui/main_window.py` 输出 0（旧占位符已移除）
    - `grep -c "self\._editor_stack" src/secnotepad/ui/main_window.py` 至少输出 2（在 _setup_central_area 使用 + _setup_editor_area 创建）
    - `python -m pytest tests/model/test_tree_model.py -x --timeout=10` 全部通过（包括 Task 1 的新测试）
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/model/test_tree_model.py -x --timeout=10</automated>
  </verify>
  <done>
    MainWindow 导航系统骨架搭建完毕: QTreeView 通过 SectionFilterProxy 显示分区，QListView 通过 PageListModel 显示页面，选择信号连接正常，编辑区可预览选中页面的 HTML 内容。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SNoteItem 数据 → setData() | 用户输入（通过 EditRole）跨越信任边界进入数据模型 |
| QTextEdit → setHtml() | HTML 内容渲染到只读预览区域 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | TreeModel.setData() / PageListModel.setData() | mitigate | 在 setData() 中验证 `isinstance(value, str) and value.strip() != ""` (D-60)。非 str 类型和空字符串返回 False，Qt 视图自动还原旧值。 |
| T-03-02 | Tampering | QTextEdit.setHtml(note.content) | accept | QTextEdit 渲染有限的 HTML4 子集——不支持 `<script>` 执行，不加载外部资源。此风险在 Phase 02 文件加密层已有缓解（攻击者无法修改加密文件内容）。在 Phase 04 中 QTextEdit 切换为可编辑模式时需重新评估此威胁。 |
| T-03-03 | DoS | 信号反馈循环 (selectionChanged → set_section → beginResetModel → selectionChanged) | mitigate | 使用 `currentChanged` 信号代替 `selectionChanged`——`currentChanged` 在 resetModel 时不一定会触发，减少信号反馈循环风险 (RESEARCH.md Pitfall 6)。此外 set_section() 使用 beginResetModel/endResetModel 而非重建模型。 |
</threat_model>

<verification>
Task 1: `python -m pytest tests/model/test_tree_model.py -x --timeout=10`
Task 2: 验证 MainWindow 导入和结构: `python -c "from src.secnotepad.ui.main_window import MainWindow"` 返回 0
</verification>

<success_criteria>
- [ ] TreeModel.flags() 返回 Qt.ItemIsEditable
- [ ] TreeModel.setData(EditRole) 支持重命名分区标题，拒绝空名称
- [ ] MainWindow 导入 SectionFilterProxy 和 PageListModel 无错误
- [ ] _setup_editor_area 创建 QStackedWidget（可切换预览/placeholder）
- [ ] _setup_navigation 创建 SectionFilterProxy + PageListModel + 信号连接
- [ ] 新建/打开笔记本后导航系统自动初始化
- [ ] 分区选择触发页面列表更新
- [ ] 页面选择触发编辑区只读预览
- [ ] 无页面选中时编辑区显示居中提示文字
- [ ] 初始状态：展开第一层，自动选中第一个子分区
- [ ] 所有已有测试仍然通过（无回归）
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-system/03-03-SUMMARY.md`
</output>
