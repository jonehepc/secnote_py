# Phase 04: 富文本编辑器 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 04-rich-text-editor
**Areas discussed:** 格式工具栏, 标题与段落, 待办列表, 缩放行为

---

## 格式工具栏

| Question | Options considered | User's choice |
|----------|--------------------|---------------|
| 格式工具栏放在哪里、什么时候可用？ | 编辑区上方固定 / 主工具栏扩展 / 浮动或内嵌条 / 你决定 | 编辑区上方固定 |
| 格式动作状态是否跟随光标当前位置更新？ | 跟随光标 / 只执行不反馈 / 仅基础反馈 / 你决定 | 跟随光标 |
| 颜色选择怎么呈现？ | 系统颜色对话框 / 预设色板 / 两者结合 / 你决定 | 系统颜色对话框 |
| 字体和字号控件要提供到什么程度？ | 系统字体+常用字号 / 只常用字号 / 自由输入字号 / 你决定 | 系统字体+常用字号 |

**Notes:** 用户选择传统桌面富文本编辑器风格：右侧编辑区上方固定格式工具栏，状态随光标同步，颜色使用系统对话框，字体使用系统字体列表与常用字号。

---

## 标题与段落

| Question | Options considered | User's choice |
|----------|--------------------|---------------|
| H1-H6 标题样式如何落到 QTextEdit/HTML？ | 语义 headingLevel / 字号加粗模拟 / 混合策略 / 你决定 | 语义 headingLevel |
| 标题控件本身怎么呈现？ | 段落样式下拉 / H1-H6 按钮组 / 菜单项为主 / 你决定 | 段落样式下拉 |
| 段落对齐按钮如何呈现？ | 四个互斥按钮 / 一个下拉按钮 / 菜单+快捷键 / 你决定 | 四个互斥按钮 |
| 正文样式和标题切换时作用范围怎么定？ | 当前段落或选区段落 / 仅当前段落 / 仅后续输入 / 你决定 | 当前段落或选区段落 |

**Notes:** 用户希望标题尽量保留语义而不是纯视觉模拟；段落样式通过下拉框选择；对齐按钮需要互斥状态并跟随当前段落。

---

## 待办列表

| Question | Options considered | User's choice |
|----------|--------------------|---------------|
| 待办事项复选框列表要做到什么程度？ | 简化符号列表 / HTML checkbox / 可点击复选框 / 你决定 | 简化符号列表 |
| 无序/有序列表和待办列表之间如何切换？ | 三种独立按钮 / 一个列表下拉 / 只放无序有序 / 你决定 | 三种独立按钮 |
| 待办项完成状态是否需要可切换？ | 不切换状态 / 快捷键切换 / 点击切换 / 你决定 | 不切换状态 |
| 缩进调整和列表嵌套边界怎么定？ | 使用 QTextEdit 标准缩进 / 严格多级列表 / 只普通段落缩进 / 你决定 | 使用 QTextEdit 标准缩进 |

**Notes:** 用户明确选择轻量待办方案，不做 HTML checkbox 或点击切换完成状态，避免把 Phase 4 扩成任务管理功能。

---

## 缩放行为

| Question | Options considered | User's choice |
|----------|--------------------|---------------|
| 缩放状态是否需要保存到文件或页面数据里？ | 仅会话级 / 每页保存 / 全局设置保存 / 你决定 | 仅会话级 |
| 缩放入口要放在哪里？ | 视图菜单+快捷键 / 格式工具栏按钮 / 两处都有 / 你决定 | 视图菜单+快捷键 |
| 切换页面后缩放如何处理？ | 保持当前缩放 / 每次重置 / 按页面记忆 / 你决定 | 保持当前缩放 |
| 是否需要显示当前缩放百分比？ | 状态栏提示 / 固定显示 / 不显示 / 你决定 | 状态栏提示 |

**Notes:** 用户把缩放定义为会话级显示偏好，不进入页面模型或加密文件；切换页面保持当前缩放，操作时用状态栏短暂反馈。

---

## Claude's Discretion

None — no discussed area was delegated to Claude discretion.

## Deferred Ideas

None — discussion stayed within phase scope.
