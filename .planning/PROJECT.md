# SecNotepad

## What This Is

一个本地加密笔记本应用，提供分区树 + 页面列表的层级笔记管理。笔记内容以加密文件形式存储在本地，用户通过密钥打开。富文本编辑、标签和搜索是核心编辑功能。面向需要安全存储笔记的个人用户使用。

## Core Value

**笔记内容始终以加密状态保存在磁盘上，只有持有密钥的用户才能解密和阅读。**

SecNotepad 的核心价值是在本地桌面环境中提供可信的加密笔记管理：用户可以用树形分区组织页面，所有笔记内容在磁盘上保持加密，仅在用户提供正确密钥后于本机内存中解密编辑。

## Requirements

### 已验证能力

- 用户可以创建、打开、保存和另存为本地 `.secnote` 加密笔记本文件。
- 打开笔记本时必须提供正确密钥，密钥错误时无法加载内容。
- 分区支持树形层级结构，页面按当前分区列表展示。
- 用户可以创建、重命名、删除分区和页面，删除含子内容的分区前会收到确认提示。

### 后续阶段能力

- Phase 4：页面内容以 QTextEdit HTML 富文本存储，支持格式工具栏、字体样式、颜色、对齐、标题、列表、缩进、撤销/重做和剪切/复制/粘贴。
- Phase 5：页面支持多标签管理，支持按标题和内容进行全文搜索并跳转到结果页面。
- Phase 6：界面完成现代化 QSS 美化、三栏布局打磨、状态栏反馈、窗口标题状态和关于对话框。

### 排除范围

- 不包含手写/绘图、云同步、多用户协作、移动端、第三方登录和插件系统。
- 不使用 QWebEngine 作为富文本编辑核心，优先保持 Qt 原生组件的桌面兼容性。

## 核心价值

**笔记内容始终以加密状态保存在磁盘上，只有持有密钥的用户才能解密和阅读。**

## 需求

### 已验证

- [x] 分区支持树形层级结构（可嵌套子分区） — 验证于 Phase 03
- [x] 用户可以创建、打开、保存加密笔记本文件（`.secnote` 格式） — 验证于 Phase 02
- [x] 用户打开笔记本时需输入密钥解密，密钥错误时无法打开 — 验证于 Phase 02
- [x] 用户可指定笔记本文件的存储位置，可在文件系统中打开已有笔记本 — 验证于 Phase 02
- [x] 用户关闭应用或笔记本时，若内容已修改则提示保存 — 验证于 Phase 02
- [x] 页面在分区下列表展示，点击分区切换对应页面列表 — 验证于 Phase 03
- [x] 用户可以创建、重命名、删除分区和页面，删除含子内容的分区时给出警告 — 验证于 Phase 03

### 进行中

- [ ] 页面支持富文本编辑（加粗、斜体、下划线、删除线、字体、字号、颜色、背景色、对齐、列表、标题）
- [ ] 用户可以为页面添加标签
- [ ] 用户可以通过关键词搜索笔记内容
- [ ] 应用界面现代化、整洁，符合桌面应用交互习惯

### 排除范围

- 手写/绘图功能 — 仅需文字编辑
- 云同步/多设备同步 — 本地单机使用
- 多用户协作 — 个人使用
- 移动端 — 仅桌面平台
- OAuth/第三方登录 — 仅本地密钥
- 插件/扩展系统 — 保持简单

## 上下文

**技术栈：** Python 3 + PySide6（Qt6 绑定），选择 PySide6 而非 PyQt6 是因 LGPL 授权对分发更友好。

**旧项目参考：** `/home/jone/projects/SafetyNotebook` — 原作者用 C++/Qt5 编写的安全笔记本 v0.1.2。核心思路延续到新项目：
- 数据模型：树形 SNoteItem（分区 + 笔记），JSON 序列化
- 加密方案：SHA-512(密码) 派生密钥 → AES 加密整体 JSON 文件
- 存储格式：单文件 `.secnote`
- 编辑器：QTextEdit 原生富文本，内容存 HTML

**新项目改进方向：**
- C++ → Python/PySide6，降低维护门槛
- 分区与页面分离展示（旧项目在同一个树视图中混合显示）
- 新增标签和搜索功能
- 现代化 UI 设计（Qt6 的样式表/QSS 美化）
- Qt5 → Qt6

**目标平台：** Windows 10+、Linux（Ubuntu/Debian 系及国产化系统如 UOS、Kylin）

## 约束

- **技术栈：** Python 3 + PySide6 + Qt6
- **加密库：** 使用 Python `cryptography` 库（而非自行实现 AES）
- **富文本：** QTextEdit 原生方案，不使用 QWebEngine（国产系统兼容性）
- **平台：** Windows + Linux 桌面，不包含 macOS
- **语言：** 代码和配置使用英文，面向用户的文档使用中文

## 关键决策

| 决策 | 理由 | 结果 |
|------|------|------|
| PySide6 而非 PyQt6 | LGPL 授权对分发无限制 | — 待验证 |
| QTextEdit 而非 QWebEngine | 国产 Linux 系统 WebEngine 兼容性不确定，原生组件更稳定 | — 待验证 |
| 整体文件加密而非逐页加密 | 实现简单，加入/退出时一次解密/加密 | — 待验证 |
| Python cryptography 库替代自定义 AES | 避免自行维护加密代码，安全性更好 | 已在 Phase 02 验证 |
| 分区树 + 页面列表分离布局 | 相比旧项目的一棵树混合显示，结构更清晰 | 已在 Phase 03 验证 |
| Ctrl+N 使用窗口级单一分发器 | 避免 Qt ambiguous shortcut，同时按焦点区分新建笔记本或新建页面 | 已在 Phase 03 接受并验证 |

## 演进规则

本文档在阶段转换和里程碑边界时演进。

**每个阶段转换后：**
1. 需求是否已失效？→ 移至"排除范围"并注明原因
2. 需求是否已验证？→ 移至"已验证"并注明阶段引用
3. 是否出现新需求？→ 添加至"进行中"
4. 是否有新决策？→ 添加至"关键决策"
5. "What This Is" 描述是否仍然准确？→ 如有偏离则更新

**每个里程碑之后：**
1. 全面审查所有章节
2. 核心价值检查 — 优先级是否仍然正确？
3. 审查排除范围 — 排除理由是否仍然有效？
4. 使用当前状态更新上下文

---
*最后更新：2026-05-09，Phase 03 完成 — 分区树、页面列表、导航 CRUD、快捷键、dirty 标记和真实 GUI 验收均通过*
