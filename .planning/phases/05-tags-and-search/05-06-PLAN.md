---
phase: 05-tags-and-search
plan: 06
type: execute
wave: 4
depends_on: ["05-03", "05-05"]
files_modified:
  - tests/ui/test_main_window_tags.py
  - tests/ui/test_main_window_search.py
  - tests/model/test_search_service.py
autonomous: false
requirements: [TAG-01, TAG-02, TAG-03, SRCH-01, SRCH-02, SRCH-03]
must_haves:
  truths:
    - "D-81: 标签入口位置、D-82: chip 添加/移除与 dirty、D-83: 页面列表不显示标签、D-84: 未选中页面禁用、D-85: trim 保留原始文本、D-86: 重复忽略大小写、D-87: 当前笔记本补全、D-88: 轻量限制、D-89: 菜单弹窗入口、D-90: 回车/按钮触发、D-91: 当前笔记本全树搜索、D-92: 字段筛选、D-93: 标题/路径/片段、D-94: HTML 转纯文本高亮片段、D-95: 结果跳转、D-96: 弹窗保持打开共同满足 Phase 05 全部成功标准"
    - "标签随 SNoteItem 序列化持久化，不新增明文落盘文件"
    - "搜索覆盖标题、正文和可选标签字段，结果可跳转且不置脏"
    - "安全边界可通过测试和 grep gate 验证"
  artifacts:
    - path: "tests/ui/test_main_window_tags.py"
      provides: "标签端到端和持久化回归测试"
    - path: "tests/ui/test_main_window_search.py"
      provides: "搜索端到端和不置脏回归测试"
    - path: "tests/model/test_search_service.py"
      provides: "搜索服务安全边界回归测试"
  key_links:
    - from: "SNoteItem.tags"
      to: "Serializer.to_json / FileService.save"
      via: "整体 JSON 加密保存链路"
      pattern: "Serializer.to_json"
    - from: "SearchDialog/SearchService"
      to: "MainWindow navigation"
      via: "result_activated -> _select_search_result"
      pattern: "已跳转到"
---

<objective>
补齐 Phase 05 的端到端回归、安全 grep gate 和全套测试门槛，确保标签与搜索在同一打开笔记本中协同工作，并满足所有用户锁定决策和需求。

Purpose: 前 5 个计划分别交付服务、组件和集成；本计划负责最终验证闭环，防止标签持久化、搜索安全和 dirty 语义之间出现交叉回归。
Output: 补充回归测试、全套 pytest 通过、Phase 05 验证命令可直接用于 `/gsd-verify-work` 前置检查。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jone/projects/secnotepad/.planning/PROJECT.md
@/home/jone/projects/secnotepad/.planning/ROADMAP.md
@/home/jone/projects/secnotepad/.planning/REQUIREMENTS.md
@/home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-CONTEXT.md
@/home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-RESEARCH.md
@/home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-VALIDATION.md
@/home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-UI-SPEC.md
@/home/jone/projects/secnotepad/src/secnotepad/model/search_service.py
@/home/jone/projects/secnotepad/src/secnotepad/ui/tag_bar_widget.py
@/home/jone/projects/secnotepad/src/secnotepad/ui/search_dialog.py
@/home/jone/projects/secnotepad/src/secnotepad/ui/main_window.py
@/home/jone/projects/secnotepad/tests/model/test_search_service.py
@/home/jone/projects/secnotepad/tests/ui/test_tag_bar_widget.py
@/home/jone/projects/secnotepad/tests/ui/test_search_dialog.py
@/home/jone/projects/secnotepad/tests/ui/test_main_window_tags.py
@/home/jone/projects/secnotepad/tests/ui/test_main_window_search.py
</context>

<source_audit>
- GOAL Phase 5 全部覆盖：页面标签管理和全文搜索。
- REQ TAG-01/TAG-02/TAG-03/SRCH-01/SRCH-02/SRCH-03 全部覆盖在本计划回归命令中。
- RESEARCH 安全域覆盖：无明文索引落盘、无敏感日志、HTML 片段转义、dirty 语义。
- CONTEXT D-81: 标签入口在右侧编辑区顶部；D-82: chip 添加/移除写入 tags 并置脏；D-83: 页面列表不显示标签；D-84: 未选中页面禁用标签区；D-85: trim 后保留原始文本；D-86: 重复忽略大小写；D-87: 当前笔记本标签补全；D-88: 空/超长轻量限制；D-89: 搜索菜单弹窗入口；D-90: 回车/按钮触发且非实时搜索；D-91: 当前笔记本全树搜索；D-92: 标题/正文/标签字段筛选；D-93: 结果标题/路径/片段；D-94: 正文 HTML 转纯文本片段并高亮；D-95: 点击结果跳转；D-96: 跳转后弹窗保持打开；本计划做综合验证；无 Deferred Ideas。
- VALIDATION 覆盖：所有命令使用 `.venv/bin/python -m pytest` 与 `QT_QPA_PLATFORM=offscreen`。
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>补充标签与搜索协同回归测试</name>
  <read_first>
    - /home/jone/projects/secnotepad/tests/ui/test_main_window_tags.py
    - /home/jone/projects/secnotepad/tests/ui/test_main_window_search.py
    - /home/jone/projects/secnotepad/tests/model/test_search_service.py
    - /home/jone/projects/secnotepad/src/secnotepad/ui/main_window.py
    - /home/jone/projects/secnotepad/src/secnotepad/model/search_service.py
  </read_first>
  <files>tests/ui/test_main_window_tags.py, tests/ui/test_main_window_search.py, tests/model/test_search_service.py</files>
  <behavior>
    - 用户在页面添加标签 `安全 项目` 后，`SearchService.search(root, "安全", SearchFields(title=False, content=False, tags=True))` 返回该页面。
    - 标签搜索结果点击跳转行为与标题/正文命中一致，只选中页面，不打开任何全局标签管理视图。
    - 保存前序列化 JSON 含 tags；搜索和跳转后序列化 JSON 不因为搜索产生额外字段、索引或历史记录。
    - 搜索正文含 HTML/脚本样式文本时，结果片段只出现纯文本与受控 `<mark>`，不出现原始 `<script>`、`style=` 或事件属性。
  </behavior>
  <action>在现有测试文件中补充端到端回归，不新增生产代码，除非测试暴露前序实现缺陷。优先复用已有 fixture；避免 brittle 的像素/布局断言。测试名称应清楚覆盖 D-87、D-91、D-94、D-95 和 dirty/security 边界。</action>
  <verify>
    <automated>cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/model/test_search_service.py tests/ui/test_main_window_tags.py tests/ui/test_main_window_search.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SearchFields(title=False, content=False, tags=True)" /home/jone/projects/secnotepad/tests/model/test_search_service.py /home/jone/projects/secnotepad/tests/ui/test_main_window_search.py` 输出至少 1 行。
    - `grep -n "Serializer.to_json" /home/jone/projects/secnotepad/tests/ui/test_main_window_tags.py /home/jone/projects/secnotepad/tests/ui/test_main_window_search.py` 输出至少 1 行。
    - `grep -n "script\|style=\|<mark>" /home/jone/projects/secnotepad/tests/model/test_search_service.py` 输出至少 2 行。
    - pytest 命令通过。
  </acceptance_criteria>
  <done>标签与搜索协同场景有自动化覆盖。</done>
</task>

<task type="auto">
  <name>执行安全 grep gate 和全套测试</name>
  <read_first>
    - /home/jone/projects/secnotepad/src/secnotepad/model/search_service.py
    - /home/jone/projects/secnotepad/src/secnotepad/ui/search_dialog.py
    - /home/jone/projects/secnotepad/src/secnotepad/ui/tag_bar_widget.py
    - /home/jone/projects/secnotepad/src/secnotepad/ui/main_window.py
    - /home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-VALIDATION.md
  </read_first>
  <files>tests/ui/test_main_window_tags.py, tests/ui/test_main_window_search.py, tests/model/test_search_service.py</files>
  <action>运行安全 grep gate 与全套测试。若 grep gate 发现问题，修复对应生产或测试文件：搜索相关代码不得有 `print(`、`logging` 输出敏感数据；`search_service.py` 不得有 `open(`、`Path(`、`sqlite`、`QSettings`；`search_dialog.py` 不得读取 `note.content` 直接展示；`tag_bar_widget.py` 不得引用 `mark_dirty` 或 `SNoteItem.content`。所有测试命令必须使用项目 `.venv/bin/python`，不要使用系统 Python。</action>
  <verify>
    <automated>cd /home/jone/projects/secnotepad && ! grep -R -n "print(\|logging\|sqlite\|QSettings\|Path(\|open(" src/secnotepad/model/search_service.py src/secnotepad/ui/search_dialog.py</automated>
    <automated>cd /home/jone/projects/secnotepad && ! grep -R -n "note.content" src/secnotepad/ui/search_dialog.py</automated>
    <automated>cd /home/jone/projects/secnotepad && ! grep -R -n "mark_dirty\|SNoteItem.*content\|content.*SNoteItem" src/secnotepad/ui/tag_bar_widget.py</automated>
    <automated>cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests -x</automated>
  </verify>
  <acceptance_criteria>
    - 三条 grep gate 命令退出码均为 0 且无匹配输出。
    - `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests -x` 通过。
    - 测试输出中没有使用系统 Python 的命令。
  </acceptance_criteria>
  <done>Phase 05 全量自动化验证通过，安全边界未被破坏。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>人工验证标签与搜索桌面交互</name>
  <read_first>
    - /home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-UI-SPEC.md
    - /home/jone/projects/secnotepad/src/secnotepad/ui/main_window.py
  </read_first>
  <files>tests/ui/test_main_window_tags.py, tests/ui/test_main_window_search.py, tests/model/test_search_service.py</files>
  <action>在自动化全套测试通过后暂停，要求用户按 `how-to-verify` 步骤检查真实桌面交互。Claude 不应让用户执行可自动化的测试；此 checkpoint 只用于视觉、焦点、弹窗保持打开、桌面快捷键和 dirty 标记的人眼确认。</action>
  <what-built>Phase 05 标签栏、标签补全、搜索弹窗、字段筛选、结果跳转和 dirty 语义已由自动化测试验证。</what-built>
  <how-to-verify>
    1. 运行 `cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=xcb .venv/bin/python -m secnotepad`（若无图形环境，可在本地桌面运行同一命令去掉 `QT_QPA_PLATFORM=xcb`）。
    2. 新建笔记本，创建分区和页面。
    3. 在右侧编辑区顶部输入标签 `Python`，再输入 `python`，确认第二次被拒绝且原 `Python` 显示形式不变。
    4. 添加中文/空格标签 `安全 项目`，确认 chip 可见，移除按钮 tooltip 为 `移除标签：安全 项目`，窗口标题出现未保存 `*`。
    5. 在正文输入包含关键词的内容，打开 `编辑(&E) → 搜索(&F)...` 或按 `Ctrl+F`。
    6. 搜索标题/正文，确认结果包含标题、路径和高亮片段；勾选标签后搜索 `安全`，确认标签命中也能跳转。
    7. 点击多个结果，确认弹窗保持打开，主窗口分区、页面和右侧编辑器同步切换。
    8. 仅搜索和跳转不应让干净笔记本出现新的 `*`；只有标签 add/remove 或正文编辑会置脏。
  </how-to-verify>
  <resume-signal>输入 `approved` 表示人工验证通过；如发现问题，请描述具体步骤和现象。</resume-signal>
  <verify>
    <automated>cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests -x</automated>
  </verify>
  <acceptance_criteria>
    - 自动化全套测试通过。
    - 人工确认标签栏位置、搜索弹窗、结果跳转和弹窗保持打开符合 UI-SPEC。
    - 人工确认搜索和跳转不误置脏。
  </acceptance_criteria>
  <done>Phase 05 可进入 `/gsd-verify-work` 或 UAT。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 标签/搜索 UI → 持久化链路 | 用户输入标签进入 SNoteItem，并随加密保存链路持久化 |
| 解密内存树 → 搜索服务/弹窗 | 页面标题、正文、标签在内存中被搜索和展示片段 |
| 搜索结果 → 导航状态 | 只读搜索结果改变主窗口选中状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | I | search persistence boundary | mitigate | grep gate 禁止外部索引/缓存/搜索历史；全套测试验证序列化无额外搜索字段。 |
| T-05-06-02 | T/I | rich text snippet | mitigate | 测试 HTML/脚本内容只以纯文本转义片段展示，不注入原始 HTML。 |
| T-05-06-03 | R/T | dirty semantics | mitigate | 集成测试和人工验证确认只有标签/正文修改置脏，搜索/跳转不置脏。 |
| T-05-06-04 | I | session cleanup | mitigate | 搜索 action 与 dialog root 在 session 清理后禁用/清空，避免旧明文对象继续可搜索。 |
</threat_model>

<verification>
- `cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/model/test_search_service.py tests/ui/test_main_window_tags.py tests/ui/test_main_window_search.py -x`
- `cd /home/jone/projects/secnotepad && QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests -x`
- 安全 grep gate 三条命令均无输出。
</verification>

<success_criteria>
- Phase 05 所有需求 TAG-01/TAG-02/TAG-03/SRCH-01/SRCH-02/SRCH-03 有自动化覆盖。
- 所有用户锁定决策 D-81: 标签入口位置、D-82: chip 与 dirty、D-83: 页面列表保持标题、D-84: 未选中禁用、D-85: trim/保留文本、D-86: 去重规则、D-87: 补全、D-88: 轻量限制、D-89: 菜单弹窗、D-90: 非实时搜索、D-91: 全笔记本范围、D-92: 字段筛选、D-93: 结果字段、D-94: 纯文本高亮片段、D-95: 跳转、D-96: 弹窗保持打开均在计划 01-06 中实现或验证。
- 无明文索引、缓存、搜索历史或敏感日志。
- 人工 UI 验证通过。
</success_criteria>

<output>
完成后创建 `/home/jone/projects/secnotepad/.planning/phases/05-tags-and-search/05-06-SUMMARY.md`。
</output>
