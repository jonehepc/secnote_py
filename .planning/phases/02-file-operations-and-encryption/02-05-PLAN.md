---
phase: 02-file-operations-and-encryption
plan: 05
type: execute
wave: 1
depends_on: [02-04]
files_modified:
  - src/secnotepad/ui/main_window.py
  - tests/ui/test_main_window.py
autonomous: true
gap_closure: true
requirements: [FILE-03, FILE-05]
must_haves:
  truths:
    - "启动应用后欢迎页显示最近打开过的文件列表"
    - "保存已有文件后，该文件路径出现在最近文件列表中"
  artifacts:
    - path: "src/secnotepad/ui/main_window.py"
      provides: "__init__ 中加载最近文件并传入欢迎页；_on_save 中更新最近文件列表"
      min_lines: 2 (changes)
    - path: "tests/ui/test_main_window.py"
      provides: "验证 init 加载和 _on_save 更新最近文件的测试"
      min_lines: 20
  key_links:
    - from: "MainWindow.__init__"
      to: "WelcomeWidget.set_recent_files"
      via: "调用 _load_recent_files() → self._welcome.set_recent_files()"
      pattern: "_load_recent_files.*set_recent_files"
    - from: "MainWindow._on_save (已有路径分支)"
      to: "MainWindow._add_recent_file"
      via: "保存成功后调用 self._add_recent_file(self._current_path)"
      pattern: "self\\._add_recent_file\\(self\\._current_path\\)"
---

<objective>
修复欢迎页最近文件列表为空的问题。

Purpose: 用户完成新建/保存/打开操作后，欢迎页的最近文件列表应当能正确显示已保存的文件。当前两个缺失导致列表始终为空：(1) 启动时未加载已保存的最近文件记录；(2) 保存到已有路径时未更新列表。

Output:
- `src/secnotepad/ui/main_window.py` — 两处修复
- `tests/ui/test_main_window.py` — 新增集成测试
</objective>

<execution_context>
@/home/jone/projects/secnotepad/.claude/get-shit-done/workflows/execute-plan.md
@/home/jone/projects/secnotepad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jone/projects/secnotepad/.planning/phases/02-file-operations-and-encryption/02-UAT.md
@/home/jone/projects/secnotepad/src/secnotepad/ui/main_window.py
@/home/jone/projects/secnotepad/tests/ui/test_main_window.py

诊断结果:
- `__init__()` 末尾缺少 `_load_recent_files()` + `set_recent_files()` 调用
- `_on_save()` 的 `if self._current_path` 分支缺少 `_add_recent_file()` 调用
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 __init__() 和 _on_save() 中最近文件加载缺失</name>
  <files>
    src/secnotepad/ui/main_window.py
    tests/ui/test_main_window.py
  </files>
  <action>
    **修复 A — `__init__()` 末尾缺少最近文件加载（UAT gap: 欢迎页最近文件列表为空）**

    在 `__init__()` 方法末尾（`_connect_actions()` 调用之后的最后一行，即第 42 行之后），添加：
    ```python
    # 加载最近文件列表到欢迎页 (D-40)
    recent = self._load_recent_files()
    self._welcome.set_recent_files(recent)
    ```

    注意：`_load_recent_files()` 已经实现了文件存在性过滤（D-42），所以设置到欢迎页的列表不会包含已删除的文件。

    **修复 B — `_on_save()` 的已有路径分支未更新最近文件（UAT gap: 保存后列表不更新）**

    在 `_on_save()` 方法的 `if self._current_path:` 分支末尾（`self.statusBar().showMessage(...)` 之后，第 390 行之后），添加：
    ```python
    self._add_recent_file(self._current_path)
    ```

    这个调用与 `_on_save_as()` 第 441 行、`_on_open_notebook()` 第 371 行的已有模式一致。

    **新增测试 — `test_main_window.py`**

    在 `TestRecentFiles` 类中追加两个测试方法：

    1. `test_init_loads_recent_files` — 验证 `__init__` 后欢迎页最近文件列表反映了 QSettings 中的内容。由于测试环境 QSettings 通常为空，验证列表存在即可（`recent_list` 是 QListWidget，检查其 count 属性）。

    2. `test_on_save_adds_recent_file` — 模拟保存到已有路径的场景：
       - 调用 `window._on_new_notebook()` 创建笔记本
       - 设置 `window._current_path` 为一个临时文件路径
       - 调用 `window._on_save()`
       - 验证该路径已出现在欢迎页的最近文件列表中
       - 清理：删除临时文件不再需要（FileService.save 需要文件存在才能写入，因此使用真实的临时文件路径；也可以 mock FileService.save 避免副作用）

    **mock 策略说明：** 为避免 `FileService.save` 写入真实文件，使用 `unittest.mock.patch` 或 `pytest.monkeypatch` 来 mock `FileService.save`，使其不执行实际写入。测试仍然验证 `_add_recent_file` 被调用且欢迎页列表被更新。
  </action>
  <verify>
    <automated>cd /home/jone/projects/secnotepad && python -m pytest tests/ui/test_main_window.py::TestRecentFiles -x -v 2>&1</automated>
  </verify>
  <done>
    - 新创建的 MainWindow 实例在窗口标题显示后，welcome widget 的 recent_list 包含已持久化的文件路径
    - 通过 `_on_save()` 保存到已有路径后，欢迎页最近文件列表立即更新
    - 两个新增测试均通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用 ↔ QSettings | QSettings 存储最近文件路径列表，用户可访问本地文件系统 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T (tampering) | QSettings recent_files key | accept | 最近文件路径属于低敏感元数据，仅包含文件路径无内容泄密风险 |
| T-02-05-02 | I (info disclosure) | 文件路径暴露 | accept | 文件路径已在欢迎页 UI 中展示给用户，无新增暴露面 |
</threat_model>

<verification>
```bash
cd /home/jone/projects/secnotepad
python -m pytest tests/ui/test_main_window.py -x -v 2>&1
# 确认所有已有测试仍然通过，新增的两个测试也通过
```
</verification>

<success_criteria>
- UAT 测试 10（欢迎页最近文件列表）从 issue 变为 pass
- 用户新建并保存文件后，重新启动应用，欢迎页显示已保存的文件
- `_on_save()` 保存到已有路径后，欢迎页最近文件列表立即更新
- 所有已有测试继续通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-operations-and-encryption/02-05-SUMMARY.md`
</output>
