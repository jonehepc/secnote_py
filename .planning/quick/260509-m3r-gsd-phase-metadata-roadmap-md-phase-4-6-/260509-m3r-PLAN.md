---
phase: quick-260509-m3r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-rich-text-editor/.gitkeep
  - .planning/phases/05-tags-and-search/.gitkeep
  - .planning/phases/06-ui-polish-and-wrap-up/.gitkeep
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
  - EDIT-06
  - EDIT-07
  - EDIT-08
  - EDIT-09
  - EDIT-10
  - EDIT-11
  - TAG-01
  - TAG-02
  - TAG-03
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - "gsd-sdk query init.phase-op 4 能识别 ROADMAP.md 中的 Phase 4，返回 phase_found true"
    - "Phase 4-6 在 .planning/phases 下都有与 Roadmap 阶段编号和 slug 对应的目录"
    - "PROJECT.md 包含 GSD 可解析的 ## Core Value 和 ## Requirements 标题，同时保留中文项目说明"
    - "gsd-sdk query validate.health 通过，或仅剩与本次 phase metadata 无关的既有警告"
  artifacts:
    - path: ".planning/phases/04-rich-text-editor/.gitkeep"
      provides: "Phase 4 可被文件系统发现的阶段目录占位"
    - path: ".planning/phases/05-tags-and-search/.gitkeep"
      provides: "Phase 5 可被文件系统发现的阶段目录占位"
    - path: ".planning/phases/06-ui-polish-and-wrap-up/.gitkeep"
      provides: "Phase 6 可被文件系统发现的阶段目录占位"
    - path: ".planning/PROJECT.md"
      provides: "英文规范标题 ## Core Value 与 ## Requirements，内容从 ROADMAP/REQUIREMENTS 提炼"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4-6 的计划字段与 phase metadata 格式可被 GSD SDK 识别"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/04-rich-text-editor/"
      via: "Phase 4 编号与目录编号 04 对齐"
      pattern: "Phase 4|04-rich-text-editor"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/05-tags-and-search/"
      via: "Phase 5 编号与目录编号 05 对齐"
      pattern: "Phase 5|05-tags-and-search"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/06-ui-polish-and-wrap-up/"
      via: "Phase 6 编号与目录编号 06 对齐"
      pattern: "Phase 6|06-ui-polish-and-wrap-up"
---

<objective>
修复 GSD phase metadata，使 ROADMAP.md 中已存在的 Phase 4-6 能被 `gsd-sdk query init.phase-op` 正确识别，并补齐 PROJECT.md 中 SDK 期望的英文规范标题。

Purpose: 当前 `.planning/phases/` 只有 01-03，而 ROADMAP.md 已定义 Phase 4 富文本编辑器、Phase 5 标签与搜索、Phase 6 界面美化与收尾；GSD SDK 很可能通过阶段目录与 Roadmap 元数据共同定位 phase，因此缺失目录或标题格式不一致会导致 `phase_found` 为 false。
Output: Phase 4-6 的阶段目录占位、PROJECT.md 的 `## Core Value` 与 `## Requirements` 规范区块、必要的 ROADMAP.md phase metadata 修正，以及验证命令结果。
</objective>

<execution_context>
@/home/jone/.claude/get-shit-done/workflows/execute-plan.md
@/home/jone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

已知状态：
- `.planning/STATE.md` 已列出 Phase 4-6，但 frontmatter 仍显示 `total_phases: 3`、`completed_phases: 3`、`status: milestone_complete`。
- `.planning/ROADMAP.md` 已定义 Phase 4-6 的目标、需求和成功标准，但 Phase 4-6 没有计划字段和计划列表。
- `.planning/phases/` 当前只有 01-03 目录，缺少 04-06 阶段目录。
- `.planning/PROJECT.md` 已有中文 `## 核心价值` 和 `## 需求`，但 quick 需求明确要求补 `## Core Value` 和 `## Requirements`；执行时应保留中文内容，同时增加 SDK 更容易识别的英文标题区块。
- 项目要求：与用户沟通、供人类阅读的 PLAN.md 等文档使用中文；Python 测试/脚本使用项目 `.venv`，不要用系统 Python。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Phase 4-6 阶段目录占位</name>
  <files>.planning/phases/04-rich-text-editor/.gitkeep, .planning/phases/05-tags-and-search/.gitkeep, .planning/phases/06-ui-polish-and-wrap-up/.gitkeep</files>
  <action>创建缺失的 phase 目录：`04-rich-text-editor`、`05-tags-and-search`、`06-ui-polish-and-wrap-up`，并在每个目录放置空 `.gitkeep`，确保 Git 能追踪空目录。目录编号必须与 ROADMAP.md 的 Phase 4-6 对齐；slug 使用英文，避免中文路径影响 GSD tooling。不要移动或重命名已有 01-03 阶段目录。</action>
  <verify>
    <automated>test -d .planning/phases/04-rich-text-editor && test -d .planning/phases/05-tags-and-search && test -d .planning/phases/06-ui-polish-and-wrap-up && test -f .planning/phases/04-rich-text-editor/.gitkeep && test -f .planning/phases/05-tags-and-search/.gitkeep && test -f .planning/phases/06-ui-polish-and-wrap-up/.gitkeep</automated>
  </verify>
  <done>`.planning/phases/` 下存在 01-06 连续编号目录，Phase 4-6 目录可被 Git 追踪。</done>
</task>

<task type="auto">
  <name>Task 2: 补齐 PROJECT.md 与 ROADMAP.md 的 GSD 可解析元数据</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>在 `.planning/PROJECT.md` 中补充英文规范标题 `## Core Value` 与 `## Requirements`。内容从现有 `## 核心价值`、`## 需求`、`.planning/ROADMAP.md` 和 `.planning/REQUIREMENTS.md` 提炼，使用中文撰写：Core Value 应明确本地加密笔记的核心价值；Requirements 应概括已验证能力、进行中 Phase 4-6 能力和排除范围。保留现有中文标题和内容，不删除历史信息。检查 `.planning/ROADMAP.md` 的 Phase 4-6：若 GSD SDK 需要 `**计划：**` 字段或计划列表才能识别 phase metadata，则为 Phase 4-6 增加 `**计划：** 0 个计划（待规划）`；不要虚构 PLAN.md 文件，也不要把 Phase 4-6 标记为完成。重点修复最可能的解析问题：Roadmap 有 phase 条目但 phases 目录缺失，以及 PROJECT 使用中文标题导致 SDK 查找 `Core Value`/`Requirements` 失败。</action>
  <verify>
    <automated>grep -q '^## Core Value$' .planning/PROJECT.md && grep -q '^## Requirements$' .planning/PROJECT.md && grep -q '^### Phase 4：富文本编辑器$' .planning/ROADMAP.md && grep -q '^### Phase 5：标签与搜索$' .planning/ROADMAP.md && grep -q '^### Phase 6：界面美化与收尾$' .planning/ROADMAP.md</automated>
  </verify>
  <done>PROJECT.md 同时包含中文说明和 `## Core Value`/`## Requirements` 英文规范标题；ROADMAP.md 的 Phase 4-6 元数据结构足以供 GSD SDK 定位待规划阶段。</done>
</task>

<task type="auto">
  <name>Task 3: 运行 GSD 健康检查与 Phase 4 识别验证</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>运行 GSD 验证命令并根据结果做必要的元数据小修正。先运行 `gsd-sdk query validate.health`，再运行 `gsd-sdk query init.phase-op 4`。若 `init.phase-op 4` 仍返回 `phase_found: false`，根据输出定位解析器期望字段，优先修正 `.planning/ROADMAP.md`、`.planning/STATE.md` 或 phase 目录命名的不一致；不要改动应用源码。若 health 检查提示 `.planning/STATE.md` 的 `total_phases` 或状态与 ROADMAP 不一致，应将 frontmatter 和阶段状态调整为 6 个总阶段、3 个已完成、当前可继续 Phase 4，避免仍显示 milestone complete 阻断下一阶段识别。</action>
  <verify>
    <automated>gsd-sdk query validate.health && gsd-sdk query init.phase-op 4 | grep -q '"phase_found"[[:space:]]*:[[:space:]]*true'</automated>
  </verify>
  <done>`gsd-sdk query validate.health` 成功；`gsd-sdk query init.phase-op 4` 输出包含 `"phase_found": true`，Phase 4 可进入后续 plan-phase。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 规划文档 → GSD SDK | Markdown 元数据被本地 CLI 解析，错误格式会导致阶段识别失败 |
| Shell 命令 → 工作区文件系统 | 执行器创建目录和修改 `.planning` 文档，只应触碰规划元数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260509-m3r-01 | Tampering | `.planning/ROADMAP.md` / `.planning/PROJECT.md` | mitigate | 只修改 phase metadata 与要求补齐的标题区块，保留既有阶段目标、需求 ID 和成功标准；验证后检查 git diff |
| T-quick-260509-m3r-02 | Denial of Service | `gsd-sdk query init.phase-op 4` | mitigate | 创建 04-06 阶段目录并对齐 ROADMAP phase 编号，运行 `validate.health` 与 `init.phase-op 4` 验证 |
| T-quick-260509-m3r-03 | Repudiation | quick 修复过程 | mitigate | 在 SUMMARY 中记录实际修改文件、验证命令和结果，便于后续追踪 |
</threat_model>

<verification>
整体检查：
1. `git diff -- .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/phases` 确认只修改 GSD metadata，不改应用源码。
2. `gsd-sdk query validate.health` 通过。
3. `gsd-sdk query init.phase-op 4` 输出包含 `"phase_found": true`。
4. 若使用 Python 辅助脚本，必须使用项目 `.venv`，不要使用系统 Python。
</verification>

<success_criteria>
- Phase 4-6 的 `.planning/phases/04-*`、`05-*`、`06-*` 目录存在且可被 Git 追踪。
- PROJECT.md 包含 `## Core Value` 和 `## Requirements`，内容为中文并从现有 ROADMAP/REQUIREMENTS 提炼。
- ROADMAP.md/STATE.md 的 phase metadata 与 6 个阶段事实一致。
- `gsd-sdk query validate.health` 成功。
- `gsd-sdk query init.phase-op 4` 返回 `phase_found true`。
</success_criteria>

<output>
完成后创建 `.planning/quick/260509-m3r-gsd-phase-metadata-roadmap-md-phase-4-6-/260509-m3r-SUMMARY.md`。
</output>
