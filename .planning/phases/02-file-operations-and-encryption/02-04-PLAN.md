---
phase: 02-file-operations-and-encryption
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
  - 02-03-PLAN.md
files_modified:
  - src/secnotepad/ui/main_window.py
  - src/secnotepad/ui/welcome_widget.py
  - tests/ui/test_main_window.py
autonomous: true
requirements:
  - FILE-02
  - FILE-03
  - FILE-04
  - FILE-05
  - CRYPT-01
  - CRYPT-02
must_haves:
  truths:
    - "打开 .secnote 文件时弹出文件选择对话框"
    - "选择文件后弹出密码输入对话框"
    - "输入正确密码后文件被解密并加载到编辑界面"
    - "输入错误密码时在对话框内显示错误提示，可重试"
    - "点击保存时直接加密保存到当前文件（无需重复输入密码）"
    - "点击另存为时可选择新路径和新密码"
    - "关闭窗口时若有未保存更改，弹出保存/不保存/取消确认框"
    - "空的未保存新建笔记本关闭时不弹出提示"
    - "欢迎页显示最近打开的文件列表（最多 5 条）"
    - "单击最近文件直接触发打开流程"
    - "文件路径不存在的条目被静默移除"
    - "窗口标题显示当前文件名，有未保存更改时带 * 标记"
  artifacts:
    - path: "src/secnotepad/ui/main_window.py"
      provides: "完整的文件操作生命周期管理"
      min_lines: 270
    - path: "src/secnotepad/ui/welcome_widget.py"
      provides: "最近文件列表交互组件"
      min_lines: 100
    - path: "tests/ui/test_main_window.py"
      provides: "扩展的主窗口测试"
      min_lines: 220
  key_links:
    - from: "ui/main_window.py"
      to: "crypto/file_service.py"
      via: "import FileService"
      pattern: "from.*file_service import FileService"
    - from: "ui/main_window.py"
      to: "ui/password_dialog.py"
      via: "import PasswordDialog"
      pattern: "from.*password_dialog import (PasswordDialog|PasswordMode)"
    - from: "ui/main_window.py"
      to: "ui/welcome_widget.py"
      via: "connect recent_file_clicked signal"
      pattern: "recent_file_clicked\\.connect"
---

<objective>
将加密层（FileService）和 UI 组件（PasswordDialog）集成到 MainWindow，实现完整的笔记本文件生命周期管理。

**Purpose:** 串联 Phase 1 的数据模型、Phase 2 的加密层和 UI 组件，使用户可以完成"新建→保存（加密写入）→打开（解密加载）→另存为→关闭保护"的完整工作流。同时实现欢迎页最近文件列表交互。

**Output:**
- `src/secnotepad/ui/main_window.py` — 增加 dirty flag、closeEvent、文件操作 handlers、最近文件管理
- `src/secnotepad/ui/welcome_widget.py` — 最近文件 QListWidget 替换占位 QLabel
- `tests/ui/test_main_window.py` — 扩展测试覆盖新功能
</objective>

<execution_context>
@/home/jone/projects/secnotepad/.claude/get-shit-done/workflows/execute-plan.md
@/home/jone/projects/secnotepad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-file-operations-and-encryption/02-CONTEXT.md
@.planning/phases/02-file-operations-and-encryption/02-RESEARCH.md
@.planning/phases/02-file-operations-and-encryption/02-UI-SPEC.md
@.planning/phases/02-file-operations-and-encryption/02-PATTERNS.md

<interfaces>
<!-- 来自 Plan 03 的 FileService 接口 -->
```python
# 从 crypto/file_service.py
class FileService:
    @staticmethod
    def encrypt(plaintext: str, password: str) -> bytes
    @staticmethod
    def decrypt(file_data: bytes, password: str) -> str
    @staticmethod
    def save(plaintext: str, path: str, password: str)
    @staticmethod
    def open(path: str, password: str) -> str       # raises ValueError on wrong pw
    @staticmethod
    def save_as(plaintext: str, path: str, password: str)
```

<!-- 来自 Plan 02 的 PasswordDialog 接口 -->
```python
# 从 ui/password_dialog.py
class PasswordMode(Enum):
    SET_PASSWORD
    ENTER_PASSWORD
    CHANGE_PASSWORD

class PasswordDialog(QDialog):
    def __init__(self, mode: PasswordMode, parent=None)
    def password(self) -> str
    def clear_password(self)
```

<!-- 来自 Plan 02 的 PasswordGenerator 接口 -->
```python
# 从 ui/password_generator.py
class PasswordGenerator(QDialog):
    def password(self) -> str
```

<!-- 现有 Serializer 接口 -->
```python
# 从 model/serializer.py
class Serializer:
    @staticmethod
    def to_json(root: SNoteItem) -> str
    @staticmethod
    def from_json(json_str: str) -> SNoteItem
```

<!-- 现有 MainWindow 模式 -->
```python
# 已有属性/方法
self._root_item: SNoteItem
self._tree_model: TreeModel
self._tree_view: QTreeView
self._list_view: QListView
self._stack: QStackedWidget
self._welcome: WelcomeWidget
self._act_save / _act_save_as / _tb_save / _tb_save_as  (disabled)
self._on_new_notebook()
self._on_open_notebook()  # 当前为桩函数
```

<!-- Existing WelcomeWidget 模式 -->
```python
# 当前
class WelcomeWidget(QWidget):
    new_notebook_clicked = Signal()
    open_notebook_clicked = Signal()
    # 有 QLabel "最近文件（功能待实现）" 占位
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 dirty flag、closeEvent、窗口标题管理</name>

  <files>
    - src/secnotepad/ui/main_window.py (modify)
  </files>

  <read_first>
    - src/secnotepad/ui/main_window.py — 现有全部代码
    - 02-CONTEXT.md — D-45~D-48 未保存检测与关闭保护决策
    - 02-UI-SPEC.md — 关闭确认对话框规格、窗口标题规则
  </read_first>

  <action>
    修改 `src/secnotepad/ui/main_window.py`，在现有基础上增加以下内容。

    **A) 在 `__init__()` 末尾新增实例变量：**

    ```python
    # ── 文件操作状态 (Phase 2) ──
    self._is_dirty: bool = False
    self._current_path: str = ""        # 当前文件路径，空=未保存
    self._current_password: str = ""    # 当前会话密码
    ```

    **B) 新增公开方法（供 Phase 3/4 调用）：**

    ```python
    # ── 脏标志管理 (D-45) ──

    def mark_dirty(self):
        """标记笔记本已修改。Phase 3（结构编辑）和 Phase 4（文本编辑）调用此方法。"""
        if not self._is_dirty and self._root_item is not None:
            self._is_dirty = True
            self._update_window_title()

    def mark_clean(self):
        """标记笔记本为已保存状态。"""
        if self._is_dirty:
            self._is_dirty = False
            self._update_window_title()
    ```

    **C) 新增窗口标题更新方法：**

    ```python
    # ── 窗口标题管理 ──

    def _update_window_title(self):
        """根据当前文件路径和脏状态更新窗口标题 (UI-06)。"""
        if self._current_path:
            filename = os.path.basename(self._current_path)
            base = f"{filename} - SecNotepad"
        else:
            base = "SecNotepad"
        if self._is_dirty:
            self.setWindowTitle(f"{base} *")
        else:
            self.setWindowTitle(base)
    ```

    需要添加 `import os` 到文件顶部。

    **D) 重写 closeEvent (D-46, D-48)：**

    ```python
    def closeEvent(self, event: QCloseEvent):
        """关闭窗口时检查未保存更改 (D-46)。

        同时处理 X 按钮关闭和 File→退出菜单 (D-46)。
        空的未保存新建笔记本不提示 (D-47)。
        """
        if self._is_dirty and self._root_item is not None:
            msg_box = QMessageBox(self)
            msg_box.setWindowTitle("未保存的更改")
            msg_box.setText("笔记本有未保存的更改。是否在关闭前保存？")
            msg_box.setIcon(QMessageBox.Question)
            btn_save = msg_box.addButton("保存(&S)", QMessageBox.AcceptRole)
            btn_discard = msg_box.addButton("不保存(&D)", QMessageBox.DestructiveRole)
            btn_cancel = msg_box.addButton("取消(&C)", QMessageBox.RejectRole)
            msg_box.setDefaultButton(btn_save)
            msg_box.exec()

            clicked = msg_box.clickedButton()
            if clicked == btn_save:
                event.ignore()
                self._on_save()
                # 保存成功后关闭
                if not self._is_dirty:
                    event.accept()
                # 如果保存取消（用户取消了另存为对话框），不关闭
            elif clicked == btn_discard:
                self._clear_session()
                event.accept()
            else:  # cancel
                event.ignore()
        else:
            self._clear_session()
            event.accept()
    ```

    需要添加 `from PySide6.QtWidgets import QMessageBox` 和 `from PySide6.QtGui import QCloseEvent`。

    **E) 新增清理方法：**

    ```python
    def _clear_session(self):
        """清理当前会话的所有敏感数据。"""
        self._current_password = ""
        self._is_dirty = False
        self._current_path = ""
    ```

    **F) 修改 `_setup_window()`，增加 Python 标准库导入：**

    在文件顶部添加：
    ```python
    import os
    ```

    并确保 Qt 导入包含：
    ```python
    from PySide6.QtWidgets import (QMainWindow, QWidget, QSplitter,
                                    QTreeView, QListView, QStackedWidget,
                                    QStyle, QVBoxLayout, QMessageBox,
                                    QFileDialog)
    from PySide6.QtGui import QAction, QKeySequence, QCloseEvent
    ```

    **G) 修改 `_on_new_notebook()`，增加启用 save/save-as 和更新标题：**

    在 `_on_new_notebook()` 方法末尾（现有代码之后）添加：
    ```python
    # Phase 2: 启用保存/另存为 (D-47: 新建笔记本不脏)
    self._is_dirty = False
    self._current_path = ""
    self._current_password = ""
    self._act_save.setEnabled(True)
    self._tb_save.setEnabled(True)
    self._act_save_as.setEnabled(True)
    self._tb_saveas.setEnabled(True)
    self._update_window_title()
    self.statusBar().showMessage("新建笔记本 - 未保存")
    ```

    注意：现有代码已有 `self.statusBar().showMessage("新建笔记本 - 未保存")`，需替换该行。
  </action>

  <acceptance_criteria>
    - `main_window.py` 顶部有 `import os` 语句
    - `main_window.py` 导入 `QMessageBox`, `QCloseEvent`, `QFileDialog`
    - `__init__` 中包含 `self._is_dirty: bool = False`
    - `__init__` 中包含 `self._current_path: str = ""`
    - `mark_dirty()` 方法存在（grep: `def mark_dirty`）
    - `mark_clean()` 方法存在（grep: `def mark_clean`）
    - `closeEvent` 方法存在（grep: `def closeEvent`）
    - `closeEvent` 中包含 `QMessageBox` 调用（grep: `QMessageBox`）
    - `_update_window_title` 方法存在
    - `_on_new_notebook` 中包含 `self._act_save.setEnabled(True)` 和 `self._act_save_as.setEnabled(True)`
  </acceptance_criteria>

  <verify>
    <automated>
      grep -c "import os" src/secnotepad/ui/main_window.py && \
      grep -c "QMessageBox" src/secnotepad/ui/main_window.py && \
      grep -c "def mark_dirty" src/secnotepad/ui/main_window.py && \
      grep -c "def closeEvent" src/secnotepad/ui/main_window.py && \
      grep -c "_act_save.setEnabled(True)" src/secnotepad/ui/main_window.py
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 实现文件操作 handlers（_on_open/_on_save/_on_save_as）和动作连接</name>

  <files>
    - src/secnotepad/ui/main_window.py (modify)
  </files>

  <read_first>
    - src/secnotepad/ui/main_window.py — 当前完整文件（含 Task 1 的修改）
    - 02-CONTEXT.md — D-36（保存流程顺序）、D-40~D-44（最近文件管理）
    - 02-UI-SPEC.md — 文件对话框规格、文案
  </read_first>

  <action>
    在 Task 1 基础上，继续修改 `main_window.py`，添加以下内容。

    **A) 新增文件操作的 import：**

    在文件顶部导入区域添加：
    ```python
    from ..crypto.file_service import FileService
    from ..model.serializer import Serializer
    from .password_dialog import PasswordDialog, PasswordMode
    ```

    **B) 实现 `_on_open_notebook()` — 打开文件完整流程 (FILE-02, D-36)：**

    替换现有的桩函数：

    ```python
    def _on_open_notebook(self):
        """打开 .secnote 文件 → 输入密码 → 解密 → 加载到编辑器 (FILE-02)。"""
        path, _ = QFileDialog.getOpenFileName(
            self, "打开加密笔记本", "",
            "SecNotepad 加密笔记本 (*.secnote)",
        )
        if not path:
            return

        # 密码对话框
        dialog = PasswordDialog(mode=PasswordMode.ENTER_PASSWORD, parent=self)
        if dialog.exec() != QDialog.Accepted:
            return
        password = dialog.password()
        dialog.clear_password()

        try:
            json_str = FileService.open(path, password)
        except ValueError as e:
            # 密码错误时在对话框内重试
            error_dialog = PasswordDialog(mode=PasswordMode.ENTER_PASSWORD, parent=self)
            # 设置错误消息
            # 通过访问内部 QLabel 设置错误文字（找 Label 控件）
            labels = error_dialog.findChildren(QLabel)
            for label in labels:
                if "请输入文件加密密码" in label.text():
                    # 在说明文字下方添加错误提示
                    pass
            # 由于无法直接注入错误到标准 PasswordDialog，使用递归重试
            # 显示错误消息后再打开新对话框
            from PySide6.QtWidgets import QMessageBox as QMB
            retry = QMB.question(
                self, "密码错误",
                "密码错误，请重试。",
                QMB.Retry | QMB.Cancel,
            )
            if retry == QMB.Retry:
                self._on_open_notebook()
            return

        # 更新数据模型
        root = Serializer.from_json(json_str)
        if self._tree_model is not None:
            self._tree_model.deleteLater()
        self._root_item = root
        self._tree_model = TreeModel(self._root_item, self)
        self._tree_view.setModel(self._tree_model)

        # 更新状态
        self._current_path = path
        self._current_password = password
        self._is_dirty = False
        self._act_save.setEnabled(True)
        self._tb_save.setEnabled(True)
        self._act_save_as.setEnabled(True)
        self._tb_saveas.setEnabled(True)

        self._stack.setCurrentIndex(1)
        self._update_window_title()
        self.statusBar().showMessage(f"已打开《{os.path.basename(path)}》")

        # 添加到最近文件 (D-44)
        self._add_recent_file(path)
    ```

    注：需要添加 `from PySide6.QtWidgets import QFileDialog`（已在 Task 1 导入）。
    同时需要 `from PySide6.QtWidgets import QLabel`（可能已有），`from PySide6.QtWidgets import QDialog`。

    **C) 实现 `_on_save()` — 保存流程 (FILE-03)：**

    ```python
    def _on_save(self):
        """保存笔记本 — 有路径则直接保存，无路径则触发另存为 (FILE-03)。"""
        if self._root_item is None:
            return

        if self._current_path:
            # 直接保存到现有文件
            json_str = Serializer.to_json(self._root_item)
            FileService.save(json_str, self._current_path, self._current_password)
            self._is_dirty = False
            self._update_window_title()
            self.statusBar().showMessage("笔记本已保存")
        else:
            # 无路径 → 触发另存为
            self._on_save_as()
    ```

    **D) 实现 `_on_save_as()` — 另存为流程 (FILE-04, D-38, D-28)：**

    ```python
    def _on_save_as(self):
        """另存为 — 选择路径 → 可选换密码 → 加密写入 (FILE-04)。"""
        if self._root_item is None:
            return

        path, _ = QFileDialog.getSaveFileName(
            self, "保存加密笔记本", "",
            "SecNotepad 加密笔记本 (*.secnote)",
        )
        if not path:
            return

        # 密码对话框（CHANGE_PASSWORD 模式：默认不换密码）
        dialog = PasswordDialog(mode=PasswordMode.CHANGE_PASSWORD, parent=self)
        if dialog.exec() != QDialog.Accepted:
            return
        new_password = dialog.password()
        dialog.clear_password()

        # 如果对话框返回空密码，使用当前密码
        if not new_password:
            new_password = self._current_password or ""

        # 如果当前无密码且用户没输入新密码，要求设置密码
        if not new_password:
            set_dialog = PasswordDialog(mode=PasswordMode.SET_PASSWORD, parent=self)
            if set_dialog.exec() != QDialog.Accepted:
                return
            new_password = set_dialog.password()
            set_dialog.clear_password()

        json_str = Serializer.to_json(self._root_item)
        FileService.save_as(json_str, path, new_password)

        # 更新状态
        self._current_path = path
        self._current_password = new_password
        self._is_dirty = False
        self._update_window_title()
        self.statusBar().showMessage("笔记本已另存为")

        # 添加到最近文件 (D-44)
        self._add_recent_file(path)
    ```

    **E) 实现最近文件管理 (D-40~D-44)：**

    在 MainWindow 中添加 QSettings 读写方法：

    ```python
    # ── 最近文件管理 (D-40~D-44) ──

    MAX_RECENT_FILES = 5
    SETTINGS_KEY = "recent_files"

    def _load_recent_files(self) -> list[str]:
        """从 QSettings 加载最近文件列表，过滤不存在的文件 (D-42)。"""
        from PySide6.QtCore import QSettings
        settings = QSettings()
        paths = settings.value(self.SETTINGS_KEY, [])
        if paths is None:
            return []
        # 过滤不存在的文件 (D-42)
        valid = [p for p in paths if isinstance(p, str) and os.path.isfile(p)]
        # 如有移除，回写更新
        if len(valid) < len(paths):
            settings.setValue(self.SETTINGS_KEY, valid)
        return valid

    def _add_recent_file(self, path: str):
        """添加文件到最近列表，去重并限制数量 (D-40, D-44)。"""
        from PySide6.QtCore import QSettings
        settings = QSettings()
        paths = settings.value(self.SETTINGS_KEY, [])
        if paths is None:
            paths = []
        # 去重: 移到顶部 (D-44)
        path = os.path.abspath(path)
        if path in paths:
            paths.remove(path)
        paths.insert(0, path)
        # 限制数量 (D-40)
        paths = paths[:self.MAX_RECENT_FILES]
        settings.setValue(self.SETTINGS_KEY, paths)

        # 更新欢迎页显示
        self._welcome.set_recent_files(paths)
    ```

    **F) 在 `_connect_actions()` 中连接保存/另存为动作：**

    ```python
    def _connect_actions(self):
        """连接菜单/工具栏动作到处理函数"""
        # ... 现有代码 ...
        self._act_new.triggered.connect(self._on_new_notebook)
        self._tb_new.triggered.connect(self._on_new_notebook)
        self._act_open.triggered.connect(self._on_open_notebook)
        self._tb_open.triggered.connect(self._on_open_notebook)

        # Phase 2: 连接保存/另存为
        self._act_save.triggered.connect(self._on_save)
        self._tb_save.triggered.connect(self._on_save)
        self._act_save_as.triggered.connect(self._on_save_as)
        self._tb_saveas.triggered.connect(self._on_save_as)
    ```

    **G) 在 `_setup_central_area()` 中连接欢迎页最近文件信号：**

    ```python
    # 在 _setup_central_area 的现有欢迎页连接之后添加：
    self._welcome.recent_file_clicked.connect(self._on_open_recent)
    ```

    并实现 handler：

    ```python
    def _on_open_recent(self, path: str):
        """单击最近文件 → 触发打开流程 (D-43)。"""
        if not os.path.isfile(path):
            # 文件已不存在，从最近列表中移除
            self._remove_recent_file(path)
            return
        # 复用打开流程：模拟从文件对话框选择了路径
        dialog = PasswordDialog(mode=PasswordMode.ENTER_PASSWORD, parent=self)
        if dialog.exec() != QDialog.Accepted:
            return
        password = dialog.password()
        dialog.clear_password()

        try:
            json_str = FileService.open(path, password)
        except ValueError:
            from PySide6.QtWidgets import QMessageBox as QMB
            QMB.warning(self, "密码错误", "密码错误，请重试。")
            return

        root = Serializer.from_json(json_str)
        if self._tree_model is not None:
            self._tree_model.deleteLater()
        self._root_item = root
        self._tree_model = TreeModel(self._root_item, self)
        self._tree_view.setModel(self._tree_model)

        self._current_path = path
        self._current_password = password
        self._is_dirty = False
        self._act_save.setEnabled(True)
        self._tb_save.setEnabled(True)
        self._act_save_as.setEnabled(True)
        self._tb_saveas.setEnabled(True)

        self._stack.setCurrentIndex(1)
        self._update_window_title()
        self.statusBar().showMessage(f"已打开《{os.path.basename(path)}》")
        # 更新最近文件顺序
        self._add_recent_file(path)
    ```

    **H) 新增 `_remove_recent_file` 辅助方法：**

    ```python
    def _remove_recent_file(self, path: str):
        """从最近列表中移除指定路径。"""
        from PySide6.QtCore import QSettings
        settings = QSettings()
        paths = settings.value(self.SETTINGS_KEY, [])
        if paths is None:
            return
        path = os.path.abspath(path)
        if path in paths:
            paths.remove(path)
        settings.setValue(self.SETTINGS_KEY, paths)
        self._welcome.set_recent_files(paths)
    ```

    **I) 在 `_on_new_notebook()` 末尾添加欢迎页最近文件更新：**

    ```python
    # 新建后更新欢迎页的最近文件（不影响当前显示，但切换回欢迎页时正确）
    recent = self._load_recent_files()
    self._welcome.set_recent_files(recent)
    ```
  </action>

  <acceptance_criteria>
    - `main_window.py` 导入 `FileService`（grep: `from.*file_service import FileService`）
    - `main_window.py` 导入 `PasswordDialog`（grep: `from.*password_dialog import`）
    - `_on_open_notebook` 中包含 `QFileDialog.getOpenFileName` 调用
    - `_on_save` 方法存在（grep: `def _on_save`）
    - `_on_save_as` 方法存在（grep: `def _on_save_as`）
    - `_add_recent_file` 方法存在（grep: `def _add_recent_file`）
    - `_load_recent_files` 方法存在（grep: `def _load_recent_files`）
    - `_on_open_recent` 方法存在（grep: `def _on_open_recent`）
    - `_connect_actions` 中包含 `self._act_save.triggered.connect`
    - `_setup_central_area` 中包含 `recent_file_clicked.connect`
  </acceptance_criteria>

  <verify>
    <automated>
      grep -c "FileService" src/secnotepad/ui/main_window.py && \
      grep -c "def _on_save" src/secnotepad/ui/main_window.py && \
      grep -c "def _on_save_as" src/secnotepad/ui/main_window.py && \
      grep -c "QFileDialog.getOpenFileName\|QFileDialog.getSaveFileName" src/secnotepad/ui/main_window.py && \
      grep -c "def _add_recent_file" src/secnotepad/ui/main_window.py
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: WelcomeWidget 最近文件 + 测试扩展</name>

  <files>
    - src/secnotepad/ui/welcome_widget.py (modify)
    - tests/ui/test_main_window.py (extend)
  </files>

  <read_first>
    - src/secnotepad/ui/welcome_widget.py — 当前实现（替换最近文件占位）
    - 02-UI-SPEC.md — 最近文件规格、文案
    - tests/ui/test_main_window.py — 现有测试模式
  </read_first>

  <action>
    **A) 修改 `src/secnotepad/ui/welcome_widget.py`：**

    替换"最近文件列表占位区域"，增加 QListWidget 和相关方法。

    ```python
    """欢迎页组件 (D-15) — 含最近文件列表 (Phase 2)"""

    from PySide6.QtCore import Signal
    from PySide6.QtWidgets import (QWidget, QVBoxLayout, QLabel,
                                    QPushButton, QListWidget)
    from PySide6.QtCore import Qt


    class WelcomeWidget(QWidget):
        """笔记本应用的欢迎页面"""

        new_notebook_clicked = Signal()
        open_notebook_clicked = Signal()
        recent_file_clicked = Signal(str)  # Phase 2: 单击最近文件 (D-43)

        def __init__(self, parent=None):
            super().__init__(parent)
            layout = QVBoxLayout(self)

            layout.addStretch()

            # 应用名称
            title_label = QLabel("SecNotepad")
            title_font = title_label.font()
            title_font.setPointSize(24)
            title_font.setBold(True)
            title_label.setFont(title_font)
            layout.addWidget(title_label)

            # 应用简介
            desc_label = QLabel("安全的本地加密笔记本")
            desc_font = desc_label.font()
            desc_font.setPointSize(12)
            desc_label.setFont(desc_font)
            layout.addWidget(desc_label)

            layout.addSpacing(20)

            # 新建笔记本按钮
            self._btn_new = QPushButton("新建笔记本")
            self._btn_new.setFixedWidth(200)
            self._btn_new.clicked.connect(self.new_notebook_clicked.emit)
            layout.addWidget(self._btn_new)

            # 打开笔记本按钮
            self._btn_open = QPushButton("打开笔记本")
            self._btn_open.setFixedWidth(200)
            self._btn_open.clicked.connect(self.open_notebook_clicked.emit)
            layout.addWidget(self._btn_open)

            layout.addSpacing(30)

            # ── 最近文件列表 (Phase 2, D-40~D-44) ──
            recent_label = QLabel("最近打开的文件")
            recent_font = recent_label.font()
            recent_font.setBold(True)
            recent_label.setFont(recent_font)
            layout.addWidget(recent_label)

            self._recent_list = QListWidget()
            self._recent_list.setMaximumHeight(150)
            self._recent_list.setMinimumWidth(300)
            self._recent_list.itemClicked.connect(self._on_recent_item_clicked)
            layout.addWidget(self._recent_list)

            # 空状态标签（初始隐藏，无最近文件时显示）
            self._empty_label = QLabel("暂无最近打开的文件")
            self._empty_label.setAlignment(Qt.AlignCenter)
            self._empty_label.setStyleSheet("color: gray;")
            self._empty_label.hide()
            layout.addWidget(self._empty_label)

            layout.addStretch()

        # ── 公开方法 ──

        def set_recent_files(self, paths: list[str]):
            """设置最近文件列表显示。

            Args:
                paths: 文件绝对路径列表（最多 5 条）。
            """
            self._recent_list.clear()
            if not paths:
                self._recent_list.hide()
                self._empty_label.show()
            else:
                self._recent_list.show()
                self._empty_label.hide()
                for path in paths:
                    self._recent_list.addItem(path)

        # ── 内部方法 ──

        def _on_recent_item_clicked(self, item):
            """单击最近文件条目，发射信号 (D-43)。"""
            self.recent_file_clicked.emit(item.text())

        # ── 属性 ──

        @property
        def new_button(self) -> QPushButton:
            """新建笔记本按钮"""
            return self._btn_new

        @property
        def open_button(self) -> QPushButton:
            """打开笔记本按钮"""
            return self._btn_open

        @property
        def recent_list(self) -> QListWidget:
            """最近文件列表"""
            return self._recent_list
        ```

    **B) 扩展 `tests/ui/test_main_window.py`：**

    在文件末尾添加新的测试类：

    ```python
    # ── 脏标志与关闭保护 (Phase 2) ──


    class TestDirtyFlag:
        """D-45, D-47: 脏标志管理"""

        def test_new_notebook_not_dirty(self, window):
            """新建笔记本后脏标志为 False (D-47)。"""
            window._on_new_notebook()
            assert window._is_dirty is False

        def test_mark_dirty_sets_flag(self, window):
            """mark_dirty() 将脏标志设为 True。"""
            window._on_new_notebook()
            window.mark_dirty()
            assert window._is_dirty is True

        def test_mark_clean_clears_flag(self, window):
            """mark_clean() 清除脏标志。"""
            window._on_new_notebook()
            window.mark_dirty()
            window.mark_clean()
            assert window._is_dirty is False

        def test_mark_dirty_no_notebook_safe(self, window):
            """无笔记本时 mark_dirty() 不报错。"""
            window.mark_dirty()  # should not raise
            assert True


    # ── 保存/另存为动作状态 ──


    class TestSaveActions:
        """FILE-03, FILE-04: 保存/另存为动作状态"""

        def test_save_disabled_initially(self, window):
            """初始状态保存按钮灰显。"""
            assert window._act_save.isEnabled() is False

        def test_save_enabled_after_new(self, window):
            """新建后保存按钮启用。"""
            window._on_new_notebook()
            assert window._act_save.isEnabled() is True

        def test_save_as_enabled_after_new(self, window):
            """新建后另存为按钮启用。"""
            window._on_new_notebook()
            assert window._act_save_as.isEnabled() is True


    # ── 窗口标题 ──


    class TestWindowTitle:
        """UI-06: 窗口标题"""

        def test_default_title(self, window):
            """默认标题为 'SecNotepad'。"""
            assert window.windowTitle() == "SecNotepad"

        def test_title_after_new_is_secnotepad(self, window):
            """新建后标题仍为 'SecNotepad'（未保存，无路径）。"""
            window._on_new_notebook()
            assert "SecNotepad" in window.windowTitle()

        def test_dirty_marker_appears(self, window):
            """脏标志改变时标题带 *。"""
            window._on_new_notebook()
            window.mark_dirty()
            assert "*" in window.windowTitle()

        def test_clean_removes_dirty_marker(self, window):
            """保存后清除 * 标记。"""
            window._on_new_notebook()
            window.mark_dirty()
            window.mark_clean()
            assert "*" not in window.windowTitle()


    # ── WelcomeWidget 最近文件 ──


    class TestRecentFiles:
        """D-40~D-44: 最近文件列表"""

        def test_welcome_has_recent_signal(self, window):
            """WelcomeWidget 有 recent_file_clicked 信号。"""
            welcome = window._stack.widget(0)
            assert hasattr(welcome, "recent_file_clicked")

        def test_recent_file_list_widget(self, window):
            """欢迎页有 QListWidget 显示最近文件。"""
            welcome = window._stack.widget(0)
            assert hasattr(welcome, "recent_list")

        def test_recent_files_set(self, window):
            """set_recent_files 更新列表内容。"""
            welcome = window._stack.widget(0)
            welcome.set_recent_files(["/path/to/test.secnote"])
            assert welcome.recent_list.count() == 1

        def test_recent_files_max_items(self, window):
            """设置超过 5 条时只显示 5 条。"""
            welcome = window._stack.widget(0)
            paths = [f"/path/{i}.secnote" for i in range(7)]
            welcome.set_recent_files(paths[:5])  # MainWindow 已过滤
            assert welcome.recent_list.count() == 5

        def test_empty_recent_files(self, window):
            """空列表显示 '暂无最近打开的文件'。"""
            welcome = window._stack.widget(0)
            welcome.set_recent_files([])
            assert welcome.recent_list.count() == 0
    ```

    确保原有测试（TestWindowSetup、TestWelcomePage、TestNewNotebook、TestSplitterLayout、TestMenuBar、TestToolBar、TestStatusBar）不被删除或修改。仅在文件末尾追加新测试。
  </action>

  <acceptance_criteria>
    - `welcome_widget.py` 导入 `QListWidget`
    - `WelcomeWidget` 类有 `recent_file_clicked = Signal(str)`
    - `WelcomeWidget` 类有 `set_recent_files` 方法
    - `WelcomeWidget` 有 `recent_list` 属性
    - 占位 QLabel "最近文件（功能待实现）" 已被移除
    - `test_main_window.py` 包含 `TestDirtyFlag`, `TestSaveActions`, `TestWindowTitle`, `TestRecentFiles` 四个新测试类
    - 新测试总数 >= 12 个
  </acceptance_criteria>

  <verify>
    <automated>
      python -m pytest tests/ui/test_main_window.py -x -q 2>&1 | tail -15
    </automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户→MainWindow | 用户通过文件操作触发加密/解密流程 |
| MainWindow→磁盘 | 保存/另存为写入加密文件 |
| 磁盘→MainWindow | 打开读取加密文件 |
| 内存中密码 | MainWindow 持有 _current_password 用于会话期间 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | D (DoS) | `closeEvent` | accept | 关闭时如果保存失败（如只读目录），用户可选择"不保存"继续关闭 |
| T-02-17 | D (Data Loss) | `closeEvent` 三按钮 | mitigate | 提供保存/不保存/取消三选项 (D-48)，防止意外丢失数据 |
| T-02-18 | I (Info Disclosure) | `_current_password` | accept | 会话期间持有密码是可行设计；关闭时通过 _clear_session() 清空 |
| T-02-19 | I (Info Disclosure) | 最近文件列表 | accept | 仅存储文件路径，不包含任何笔记内容；QSettings 是本地存储 |
| T-02-20 | T (Tampering) | 最近文件路径 | accept | D-42 加载时检查文件存在性，不存在的条目静默移除 |
</threat_model>

<verification>
```bash
# 导入验证
python -c "
from src.secnotepad.ui.main_window import MainWindow
from src.secnotepad.ui.welcome_widget import WelcomeWidget
import inspect
src = inspect.getsource(MainWindow)
assert 'mark_dirty' in src
assert 'closeEvent' in src
assert '_on_save' in src
assert '_on_save_as' in src
print('All methods present')
"

# 单元测试
python -m pytest tests/ui/test_main_window.py -x -q
```
</verification>

<success_criteria>
- MainWindow 具备完整的文件操作生命周期（新建→保存→打开→另存为→关闭保护）
- WelcomeWidget 最近文件列表可交互
- 关闭保护在脏状态时弹出三按钮确认框
- 窗口标题正确反映当前文件路径和脏状态
- 保存/另存为按钮在加载笔记本后启用
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-operations-and-encryption/02-04-SUMMARY.md`
</output>
